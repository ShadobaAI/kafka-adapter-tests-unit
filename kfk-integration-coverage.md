# Покрытие API обработки кфкИнтеграция

Модуль: `Обр_кфкИнтеграция_МО`, серверные тесты расширения `unit`.

61 зарегистрированный метод, 130 сценариев с учетом параметров. В матрице перечислены все 102 экспортных метода исходной обработки. Стрелка означает вызов через служебный метод тестового модуля. Это проверка соответствия исходного кода сценария API, а не измерение покрытия во время выполнения: ошибка раннего утверждения или внешней компоненты может прервать сценарий.

У функций проверяются тип и значение ответа, у процедур — наблюдаемый эффект либо состояние объекта. Для переменных объекта `Коннектор`, `РежимЧтенияДвоичныхДанных`, `РежимАсинхроннойОтправки`, `ТаймаутОжиданияОтвета` предусмотрены отдельные сценарии и тестовый метод `ют_СостояниеДляТестирования()` в заимствованной обработке.

Стенд: общий dev/test-кластер node1/node2 (`localhost:29091,localhost:29092`), Schema Registry (`http://localhost:38081`). Подробности запуска и очистки — в [README](README.md).

## Изменения для коннектора 1.9.3

Сравнение обработки в репозитории адаптера выполнено от `91654fe52e99d07f3715e9fa36de1ee9b5d94a59` до состояния с коммитом `93775a6`. Измененные контракты сверены с обработкой, внедренной в проект `base`.

- `ОписатьГруппуКонсьюмеров`: существующие проверки обоих представлений ответа проверяют передачу идентификатора своей группы; `member_count` проверяется как строка `"0"` согласно JSON коннектора.
- `УстановитьПозицииЧтения`: входные поля исправлены на `topic/offset/partition`; позиции задаются до инициализации консьюмера, эффект проверяется после подписки и распределения партиций. Коннектор подключает callback позиций только при инициализации.
- `ЧитатьПакетСообщений`: проверяются все четыре комбинации флагов Base64 и десериализации.
- `AVRO_ПередаетФорматИДекодируетЗапись`: 16 сценариев передачи через Kafka. Проверяются OCF, raw, confluent, регистр и обозначения формата, точные байты raw и заголовка Confluent с schema ID 66051, вход в двоичном виде и Base64, выход JSON и двоичные данные. Для OCF схема извлекается из контейнера; двоичный результат сравнивается с raw-записью без контейнера. Проверяется отсутствие автоматического распознавания Base64.
- `СохранитьСхемуAVRO_ОбновляетСхемуСПовторнымИменем`: обновление схемы строкой и структурой, передача массива из двух записей через Kafka и декодирование по новой схеме.
- `AVRO_ОтклоняетНекорректныеДанные`: 9 сценариев для извлечения схемы и обоих режимов декодирования. Для пустого входа, некорректного Base64 и недопустимого типа проверяются тип `Строка`, пустое значение и точный текст ошибки.

## Актуальный полный прогон: коннектор 1.9.3, 04.09.2026

После обновления базы выполнен полный прогон модуля: jobId `496b2c7b-bc5e-4ddf-b1ca-7b202c8df082`, **130 сценариев: 121 успешный, 8 failure, 1 error, 0 пропусков**. Все 28 добавленных сценариев успешны. Проходят синхронная доставка и декодирование AVRO, оба варианта описания группы, оба метода установки позиций и четыре комбинации пакетного чтения.

Оставшиеся 9 проблем не относятся к новым сценариям:

| Проверка | Количество | Результат |
| --- | ---: | --- |
| СброситьОфсетыГруппыКонсьюмеров, earliest/latest | 2 | Булево Ложь вместо Истина. |
| УдалитьГруппуКонсьюмеров | 1 | Аварийное завершение внешней компоненты при отдельном вызове API. Общая очистка этот метод не вызывает. |
| Асинхронная отправка AVRO/Protobuf | 2 | Число -1 вместо 0. |
| Двоичный ответ ДекодироватьСообщениеProtobuf | 1 | Строка вместо ДвоичныеДанные. |
| ПолучитьЗаголовкиСообщения, оба представления | 2 | JSON-объект/Соответствие вместо ожидаемого массива key/value. |
| Временная метка сообщения | 1 | Значение соответствует секундам, ожидаются миллисекунды; сравнение с ISO не проходит. |

Текущий отчет: `C:\EDT\tmp\edt-mcp-yaxunit\YAxUnit_-_Kafka_Unit_6d3c135e5933_0736eef1a0168acfb87db0bdb0194efab7e4bfb5\report.md`. Он перезаписывается runner при следующем запуске с тем же фильтром. Результат полного прогона — FAILED; оставшиеся проверки не отключены и не ослаблены.

Итоговая диагностика EDT: четыре прежних маркера `bsl-legacy-check-dynamic-feature-access` в трех местах (УстановитьПозициюЧтения, Слушать, УстановитьТаймаутОчисткиКонсьюмера). Новых диагностик нет. Устаревшие методы намеренно проверяются по требованию полного покрытия экспортного API, подавления не добавлены.

## Матрица

| Экспортный метод API | Сценарий / путь вызова |
| --- | --- |
| `ПолучитьИнформациюОКластере` | `ПолучитьИнформациюОКластере_ВозвращаетДваБрокера` |
| `ПолучитьИнформациюОБрокере` | `ПолучитьИнформациюОБрокере_ВозвращаетАдрес` |
| `ПроверитьДоступностьБрокера` | `ПроверитьДоступностьБрокера_ВозвращаетИстину` |
| `ПолучитьНастройкиБрокера` | `ПолучитьНастройкиБрокера_ВозвращаетКонфигурациюУзла` |
| `УстановитьНастройкиБрокера` | `УстановитьНастройкиБрокера_ОтклоняетНедопустимоеЗначение` |
| `ПолучитьВозможностиБиблиотеки` | `ПолучитьВозможностиБиблиотеки_ВозвращаетВозможности` |
| `ПолучитьСписокТопиков` | `УдалитьТопик_УдаляетТолькоТестовыйТопик` |
| `ПолучитьМетаданныеТопика` | `СоздатьТопик_СоздаетДвеПартиции` |
| `СоздатьТопик` | `СоздатьТопик_ПовторноеСозданиеВозвращаетЛожь` |
| `УдалитьТопик` | `УдалитьТопик_УдаляетТолькоТестовыйТопик` |
| `УдалитьЗаписи` | `УдалитьЗаписи_ОставляетЗаписьНаУказанномСмещении` |
| `ПолучитьНастройкиТопика` | `НастройкиТопика_ИзменяютСрокХранения` |
| `УстановитьНастройкиТопика` | `НастройкиТопика_ИзменяютСрокХранения` |
| `ПолучитьРазмерТопика` | `ПолучитьРазмерТопика_СчитаетТелоИКлюч` |
| `ПолучитьОтставаниеКонсьюмера` | `ПолучитьОтставаниеКонсьюмера_УчитываетЗафиксированнуюПозицию` |
| `ПолучитьКонсьюмеровТопика` | `ПолучитьКонсьюмеровТопика_НаходитГруппуСПозицией` |
| `ПолучитьСписокГруппКонсьюмеров` | `ПолучитьСписокГруппКонсьюмеров_СодержитТестовуюГруппу` |
| `УдалитьГруппуКонсьюмеров` | `УдалитьГруппуКонсьюмеров_УдаляетСохраненныеСмещения` |
| `СброситьОфсетыГруппыКонсьюмеров` | `СброситьОфсетыГруппыКонсьюмеров_МеняетЗафиксированнуюПозицию` |
| `ОписатьГруппуКонсьюмеров` | `ОписатьГруппуКонсьюмеров_ВозвращаетУказаннуюГруппу` |
| `ПолучитьГраницыПартиции` | `ПолучитьГраницыПартиции_ВозвращаетСмещенияДвухЗаписей` |
| `ПолучитьКоличествоСообщенийВПартиции` | `ПолучитьГраницыПартиции_ВозвращаетСмещенияДвухЗаписей` |
| `НазначитьПартиции` | `ОтменитьНазначение_ОчищаетПартиции` → `ПодготовитьКонсьюмера` |
| `ПолучитьНазначение` | `ОтменитьНазначение_ОчищаетПартиции` |
| `ОтменитьНазначение` | `ОтменитьНазначение_ОчищаетПартиции` |
| `ПерейтиКНачалу` | `ПерейтиКНачалу_ПовторноЧитаетПервуюЗапись` |
| `ПерейтиККонцу` | `ПерейтиКНачалу_ПовторноЧитаетПервуюЗапись` |
| `ПерейтиКВременнойМетке` | `ПерейтиКВременнойМетке_ПовторноЧитаетЗапись` |
| `ЗарегистрироватьСхему` | `ЗарегистрироватьСхему_ПовторноВозвращаетТотЖеИдентификатор` |
| `ПолучитьСхемуПоИД` | `ПолучитьСхемуПоИД_ВозвращаетИсходнуюСхему` |
| `ПолучитьПоследнююСхему` | `ПолучитьПоследнююСхему_ВозвращаетВерсиюСубъекта` |
| `ПолучитьВерсииСхемы` | `ПолучитьВерсииСхемы_ВозвращаетПервуюВерсию` |
| `УдалитьСхему` | `УдалитьСхему_УдаляетВерсию` |
| `УстановитьТаймаутАдминОпераций` | `ТаймаутыКомпоненты_ПринимаютМиллисекунды` |
| `ДобавитьACL` | `ACL_СоздаетОписываетИУдаляетТестовоеРазрешение` |
| `ОписатьACL` | `ACL_СоздаетОписываетИУдаляетТестовоеРазрешение` |
| `УдалитьACL` | `ACL_СоздаетОписываетИУдаляетТестовоеРазрешение` |
| `ПолучитьСообщениеОбОшибке` | `УдалитьСхему_УдаляетВерсию` |
| `УстановитьКаталогЛогов` | `УстановитьКаталогЛогов_СохраняетПутьИФормат` |
| `УстановитьРежимОтладки` | `УстановитьРежимОтладки_ОбъединяетСобытия` |
| `УстановитьСборСтатистики` | `УстановитьСборСтатистики_СохраняетИнтервал` |
| `ВерсияКомпоненты` | `Инициализация_СоздаетКомпонентуИЗначенияПоУмолчанию` |
| `ПолучитьМетрикиПродюсера` | `Метрики_УчитываютОтправкуЧтениеИСброс` |
| `ПолучитьМетрикиКонсьюмера` | `Метрики_УчитываютОтправкуЧтениеИСброс` |
| `СброситьМетрики` | `Метрики_УчитываютОтправкуЧтениеИСброс` |
| `УстановитьПараметр` | `Подписаться_ПолучаетСообщениеТопика` |
| `УстановитьПараметры` | `УстановитьПараметры_СохраняетКоллекцию` |
| `ПолучитьПараметры` | `ПолучитьПараметры_ВозвращаетУстановленныйПараметр` |
| `УстановитьПартишионер` | `УстановитьПартишионер_ПринимаетСтратегию` |
| `ТипПартишионера_ПоКлючу` | `ТипыПартишионера_ВозвращаютИмена` |
| `ТипПартишионера_Murmur2` | `ТипыПартишионера_ВозвращаютИмена` |
| `ТипПартишионера_FNV1A` | `ТипыПартишионера_ВозвращаютИмена` |
| `ТипПартишионера_СлучайнаяПартиция` | `ТипыПартишионера_ВозвращаютИмена` |
| `ИнициализироватьПродюсера` | `Слушать_ВозвращаетПрочитаннуюЗапись` → `ПодготовитьПродюсера` |
| `УстановитьРежимАсинх` | `УстановитьРежимАсинх_ИзменяетТолькоСвойОбъект` |
| `УстановитьТаймаутОчисткиПродюсера` | `ТаймаутыКомпоненты_ПринимаютМиллисекунды` |
| `ОтправитьСообщение` | `ОтправитьСообщение_СохраняетТелоКлючИЗаголовки` |
| `ОтправитьПакетСообщений` | `ОтправитьПакетСообщений_ВозвращаетКоличество` |
| `ОтправитьПакетСРезультатом` | `ОтправитьПакетСРезультатом_ВозвращаетОтчетОДоставке` |
| `ОстановитьПродюсера` | `ОтправитьПакетСообщений_ВозвращаетКоличество` |
| `ИнициализироватьТранзакционногоПродюсера` | `ЗафиксироватьТранзакциюKafka_ПубликуетЗапись` → `ПодготовитьТранзакцию` |
| `НачатьТранзакциюKafka` | `ОтменитьТранзакциюKafka_СкрываетОтмененнуюЗапись` |
| `ЗафиксироватьТранзакциюKafka` | `ЗафиксироватьТранзакциюKafka_ПубликуетЗапись` |
| `ОтменитьТранзакциюKafka` | `ОтменитьТранзакциюKafka_СкрываетОтмененнуюЗапись` |
| `ОтправитьОфсетыВТранзакцию` | `ОтправитьОфсетыВТранзакцию_СохраняетПозициюГруппы` |
| `ПолучитьСмещенияГруппыКонсьюмеров` | `ЗафиксироватьСмещение_СохраняетПозициюГруппы` |
| `УстановитьПозицииЧтения` | `УстановитьПозицииЧтения_НачинаетСоВторойЗаписи` |
| `ИнициализироватьКонсьюмера` | `ОтменитьНазначение_ОчищаетПартиции` → `ПодготовитьКонсьюмера` |
| `УстановитьРежимЧтенияДвоичныхДанных` | `УстановитьРежимЧтенияДвоичныхДанных_СохраняетЗначение` |
| `УстановитьТаймаутОжидания` | `УстановитьТаймаутОжидания_СохраняетЧисло` |
| `Подписаться` | `Подписаться_ПолучаетСообщениеТопика` |
| `ПрочитатьСообщение` | `ПрочитатьСообщение_ПустаяПартицияВозвращаетЛожь` |
| `ЧитатьПакетСообщений` | `ЧитатьПакетСообщений_ВозвращаетЗапись` |
| `ПрочитатьСообщениеПоСмещению` | `УстановитьПартишионер_ПринимаетСтратегию` |
| `ДанныеПрочитанногоСообщения` | `ПрочитатьСообщение_ВозвращаетДанныеЗаписи` |
| `ПолучитьДанныеСообщения` | `Подписаться_ПолучаетСообщениеТопика` |
| `ПолучитьКлючСообщения` | `ДвоичныеФорматы_СохраняютИПередаютДанные` |
| `ПолучитьЗаголовкиСообщения` | `ПолучитьЗаголовкиСообщения_ВозвращаетЗаголовкиЗаписи` |
| `ПолучитьТопикСообщения` | `ПрочитатьСообщение_ВозвращаетДанныеЗаписи` |
| `ПолучитьИдентификаторБрокераСообщения` | `ПрочитатьСообщение_ВозвращаетДанныеЗаписи` |
| `ПолучитьВременнуюМеткуСообщения` | `ПрочитатьСообщение_ВозвращаетДанныеЗаписи` |
| `ПолучитьВременнуюМеткуСообщенияISO` | `ВременныеМетки_СоответствуютПрочитаннойЗаписи` |
| `ПолучитьРазделСообщения` | `ПрочитатьСообщение_ВозвращаетДанныеЗаписи` |
| `ПолучитьСмещениеСообщения` | `ПрочитатьСообщение_ВозвращаетДанныеЗаписи` |
| `ЗафиксироватьСмещение` | `ЗафиксироватьСмещение_СохраняетПозициюГруппы` |
| `ОстановитьКонсьюмера` | `ПослеКаждогоТеста` |
| `СохранитьСхемуAVRO` | `СохранитьСхемуAVRO_ОбновляетСхемуСПовторнымИменем`, `AVRO_ПередаетФорматИДекодируетЗапись`, `ДвоичныеФорматы_СохраняютИПередаютДанные` |
| `ПолучитьСхемуAVRO` | `AVRO_ПередаетФорматИДекодируетЗапись`, `AVRO_ОтклоняетНекорректныеДанные`, `СохранитьСхемуAVRO_ОбновляетСхемуСПовторнымИменем`, `ДвоичныеФорматы_СохраняютИПередаютДанные` |
| `ПреобразоватьВФорматAVRO` | `AVRO_ПередаетФорматИДекодируетЗапись`, `СохранитьСхемуAVRO_ОбновляетСхемуСПовторнымИменем`, `ДвоичныеФорматы_СохраняютИПередаютДанные` |
| `ОтправитьСообщениеAVRO` | `AVRO_ПередаетФорматИДекодируетЗапись`, `СохранитьСхемуAVRO_ОбновляетСхемуСПовторнымИменем`, `ДвоичныеФорматы_СохраняютИПередаютДанные` |
| `СохранитьФайлAVRO` | `ДвоичныеФорматы_СохраняютИПередаютДанные` |
| `ДекодироватьСообщениеAVRO` | `AVRO_ПередаетФорматИДекодируетЗапись`, `AVRO_ОтклоняетНекорректныеДанные`, `СохранитьСхемуAVRO_ОбновляетСхемуСПовторнымИменем`, `ДвоичныеФорматы_СохраняютИПередаютДанные` |
| `СохранитьСхемуProtobuf` | `ДвоичныеФорматы_СохраняютИПередаютДанные` |
| `ПреобразоватьВФорматProtobuf` | `ДвоичныеФорматы_СохраняютИПередаютДанные` |
| `ОтправитьСообщениеProtobuf` | `ДвоичныеФорматы_СохраняютИПередаютДанные` |
| `СохранитьФайлProtobuf` | `ДвоичныеФорматы_СохраняютИПередаютДанные` |
| `ДекодироватьСообщениеProtobuf` | `ДвоичныеФорматы_СохраняютИПередаютДанные` |
| `УстановитьПозициюЧтения` | `УстановитьПозицииЧтения_НачинаетСоВторойЗаписи` |
| `Слушать` | `Слушать_ВозвращаетПрочитаннуюЗапись` |
| `УстановитьТаймаутОчисткиКонсьюмера` | `ТаймаутыКомпоненты_ПринимаютМиллисекунды` |
| `Пауза` | `Пауза_ПриостанавливаетВыполнение` |
| `ПолучитьИдентификаторПроцесса` | `Инициализация_СоздаетКомпонентуИЗначенияПоУмолчанию` |

## Полный прогон 04.09.2026 до исправления очистки

Полный прогон через unit-edt: jobId `f1e0aa39-2ed7-4064-aedb-340ff24fcfbc`, 102 сценария, 79 успешных, 23 с проблемами, пропусков нет. Schema Registry — 9/9, ACL — 2/2, инициализация и режимы объекта — 7/7. Проверки создания топика, изменения его настроек и поиска по корректной временной метке прошли после уточнения фикстур.

В JUnit 11 сценариев содержат failure, 15 — error, из них 3 содержат оба элемента. Поэтому число успешных сценариев рассчитано по testcase без failure/error/skipped: 79. Сводка EDT-MCP показывает 76, вычитая пересекающиеся категории дважды. Исходный отчет не изменялся.

Локальный отчет: `C:\EDT\tmp\edt-mcp-yaxunit\YAxUnit_-_Kafka_Unit_6d3c135e5933_0736eef1a0168acfb87db0bdb0194efab7e4bfb5\report.md`. Файл в каталоге runner перезаписывается следующим прогоном с тем же фильтром.

### Расхождения на момент исторического прогона

| API / сценарий | Наблюдаемое поведение |
| --- | --- |
| УдалитьГруппуКонсьюмеров | Аварийное завершение внешней компоненты. Воспроизводится отдельным тестом и при очистке фикстур. Ошибки очистки затрагивают также успешно проверенные смещения, lag, список групп, подписку и транзакционные позиции. Некоторые тестовые группы и ресурсы могут остаться после аварии. |
| ОписатьГруппуКонсьюмеров | При отключенной десериализации возвращается Булево Ложь вместо JSON-строки, при включенной — ошибка разбора. В исходной обертке ИдГруппы не передается в вызов компоненты. |
| СброситьОфсетыГруппыКонсьюмеров | Возвращает Ложь для earliest и latest при подготовленной группе и инициализированном консьюмере. |
| УстановитьПозицииЧтения / УстановитьПозициюЧтения | Пакетный метод возвращает Ложь; устаревший одиночный возвращает Истина, но следующее чтение дает первую запись вместо второй. |
| ОтправитьСообщениеAVRO / ОтправитьСообщениеProtobuf | В асинхронном режиме возвращается -1 вместо ожидаемого 0. |
| ДекодироватьСообщениеAVRO | Полученная строка не разбирается как JSON. Сохранение схемы, преобразование, запись файла и синхронная доставка двоичных данных доходят до этой проверки. |
| ДекодироватьСообщениеProtobuf | При ВозвращатьJSON = Ложь возвращает Строка вместо ДвоичныеДанные. Проверка типа больше не помещает управляющие символы в ошибку YAxUnit/XDTO. |
| ПолучитьЗаголовкиСообщения | Десериализованный ответ имеет тип Соответствие, хотя описание API обещает массив заголовков. JSON также не соответствует ожидаемому массиву key/value. |
| ПолучитьВременнуюМеткуСообщения | Значение соответствует секундам, хотя описание API указывает миллисекунды; сравнение с ISO-представлением выявляет разницу в 1000 раз. Поиск по времени отдельно проверен с меткой в миллисекундах от платформы. |

Ожидания, выявляющие эти расхождения, не отключены. Все проверки типа и значения в падающем сценарии нельзя считать успешно выполненными. Для успешного полного прогона требуется разбор и исправление перечисленных контрактов в обработке/внешней компоненте; продуктовые репозитории в рамках этой задачи не изменялись.

### EDT и структура тестов

После изменений проверены CommonModule.Обр_кфкИнтеграция_МО и DataProcessor.кфкИнтеграция. Остались пять маркеров `bsl-legacy-check-dynamic-feature-access` («Метод устарел [Сервер]») в трех местах: УстановитьПозициюЧтения, Слушать, УстановитьТаймаутОчисткиКонсьюмера. Они относятся к намеренно проверяемым экспортным методам; пользователь запросил покрытие всего API. Подавления диагностики не добавлены. Прочих диагностик в итоговой проверке нет.

Проверены регистрация всех 58 тестовых методов, отсутствие дубликатов, иерархия fluent-цепочек и ограничение длины строки 120 символами. Каждому экспортному API сопоставлен сценарий в таблице выше.

## Исправление очистки 04.09.2026

В предыдущем полном прогоне ошибка в ПослеКаждогоТеста была вызвана лишним явным удалением группы после удаления ее единственного топика. На node1/node2 проверено отдельным экспериментом: неактивная группа с зафиксированным смещением существовала до удаления тестового топика и автоматически исчезла после него. Временный топик и группа эксперимента удалены.

ПослеКаждогоТеста теперь удаляет топик и до 20 раз проверяет отсутствие своей группы в ПолучитьСписокГруппКонсьюмеров(). Повторный вызов УдалитьГруппуКонсьюмеров() исключен из общей очистки. Отдельный тест этого API сохранен.

После обновления базы выполнен фокусный прогон jobId `3fcc12ec-50c2-49c0-8c40-727a718c55f8`: **9/9 успешных**, ошибок и пропусков нет. Проверены ЗафиксироватьСмещение, lag (2 варианта), консьюмеры топика (2), список групп (2), отправка смещений в транзакцию и подписка. Прежние ошибки очистки этих сценариев устранены. Полный набор после этой правки повторно не запускался; приведенная выше статистика полного прогона относится к предыдущей версии очистки.
