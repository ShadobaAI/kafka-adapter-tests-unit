# Реализация тестов кфкФоновыеОперацииСлужебный

Дата: 11.09.2026. Статус: **основной запуск восстановлен, A02 прошел 6/6, A03 на отдельном стенде — 2/2; зарегистрировано 2227 сценариев. Последний общий прогон — 2219 сценариев: 2158 пройдено, 61 падение, 0 ошибок и пропусков. Продукт не изменялся. Замер получен, но ограничение API не позволяет определить полное покрытие; полный план не завершен**. Результаты повторной проверки и состояние диагностики приведены в последнем разделе; предыдущие разделы сохраняют историю.

## Версия и границы

- План: [kfk-background-operations-test-plan.md](kfk-background-operations-test-plan.md).
- Живой исходник: unit-edt, проект `base`, `CommonModules/кфкФоновыеОперацииСлужебный/Module.bsl`.
- Актуальный хэш EDT base: `6fdf7dc94e2cfc19`, 5262 строки; подтверждён чтением исправленной ИнициализироватьКэшРаспределенияЧтения. Исторический хэш до исправления: `cfbe824c47a32eb0`, 65 процедур и 97 функций. Полная побайтовая сверка с canonical checkout и повторная инвентаризация всех методов не выполнялись.
- Изменения принадлежат только `unit`: новый тестовый общий модуль `ОМ_кфкФоновыеОперацииСлужебный` и заимствованный `кфкФоновыеОперацииСлужебный` с семью минимальными экспортными обёртками. Заимствование и регистрация объектов изменяют `Configuration.mdo` через EDT.
- Обёртки непосредственно вызывают оригинальные методы; алгоритмы продукта в них не скопированы. Разрешение ссылок проверено диагностикой EDT; вызовы исходных методов через обёртки подтверждены успешным выполнением тестов в 1С.
- Все 47 сценариев вызываются напрямую в серверном сеансе теста. Первые 33 не создают записи БД. Новые 14 проверяют запись позиции операции и изолированы транзакцией набора YAxUnit; используются уникальные UUID операции, раздела и потоков. Фоновые задания, сетевые ресурсы и изменение функциональных опций не требуются.

## Наблюдаемые результаты

| ID | Проверка |
|---|---|
| A11 / НовоеОписаниеПравилОбмена | Новое описание: состав и значения; независимость экземпляров |
| A12 / ПредставленияИдентификатораОпераций | Девять UUID, их уникальность, порядок и представления |
| A12 / КлючиСлужебныхОпераций | Тип фиксированного массива, полный состав и порядок ключей; отдельные состояния опций пока не подготовлены |
| B06 / ПустаяТаблицаУказателейПотоков | Три колонки, типы и квалификаторы чисел, индекс UUID |
| B06 / ДобавитьУказательПотока | UUID, метка, нулевой статус, сохранность существующей строки |
| B06 / ЗафиксироватьУказатели | Пустая таблица, пустой массив потоков, отсутствующий UUID, несколько совпадений; статусы -1/0/1 |
| G04 / РассчитатьУказательУдаления | Сроки -1/-2/0/0.5/1, ограничение логами ниже/на/выше порога; ошибка для -3 |
| I06 / ДополнитьТекстЖурнала | Пустые и пробельные аргументы, сохранение единственного текста, порядок и разделитель |
| I08 / ХэшСуммаТела | Контрольные векторы пустого тела и abc, строка и байты UTF-8; повторяемость и изменение кириллицы |

I08 пока не содержит отдельного известного контрольного вектора кириллицы и изменения ровно одного байта. A12 не содержит отдельных запусков при выключенных функциональных опциях. Эти пункты плана не отмечены выполненными.

## Матрица сценариев

Строка теста относится к `src/CommonModules/ОМ_кфкФоновыеОперацииСлужебный/Module.bsl`.
Строки продукта — расположение проверяемых методов, **не результат профилирования**.
Сочетания параметров зарегистрированы декларативно, у каждого набора указаны категория, объект и операция, у теста — поведение.

| ID | Тест | Параметры | Строка теста | Строки продукта | Результат |
|---|---|---|---:|---|---|
| A11 | `НовоеОписаниеПравилОбмена_ВозвращаетЗначенияПоУмолчанию` | без параметров | 212 | 1047–1060 | Пройден |
| A11 | `НовоеОписаниеПравилОбмена_СоздаетНезависимыеСтруктуры` | без параметров | 227 | 1047–1060 | Пройден |
| I06 | `ДополнитьТекстЖурнала_СохраняетЕдинственныйТекст` [1] | `"", "", ""` | 249 | 5140–5155 | Пройден |
| I06 | `ДополнитьТекстЖурнала_СохраняетЕдинственныйТекст` [2] | `"", " новое ", " новое "` | 249 | 5140–5155 | Пройден |
| I06 | `ДополнитьТекстЖурнала_СохраняетЕдинственныйТекст` [3] | `" прежнее ", "", " прежнее "` | 249 | 5140–5155 | Пройден |
| I06 | `ДополнитьТекстЖурнала_СохраняетЕдинственныйТекст` [4] | `"   ", " новое ", " новое "` | 249 | 5140–5155 | Пройден |
| I06 | `ДополнитьТекстЖурнала_СохраняетЕдинственныйТекст` [5] | `" прежнее ", "   ", " прежнее "` | 249 | 5140–5155 | Пройден |
| I06 | `ДополнитьТекстЖурнала_СохраняетЕдинственныйТекст` [6] | `" ", "  ", "  "` | 249 | 5140–5155 | Пройден |
| I06 | `ДополнитьТекстЖурнала_ДобавляетНовоеСообщениеВНачало` [1] | `"прежнее", "новое"` | 264 | 5140–5155 | Пройден |
| I06 | `ДополнитьТекстЖурнала_ДобавляетНовоеСообщениеВНачало` [2] | `" прежнее ", " новое "` | 264 | 5140–5155 | Пройден |
| B06 | `ПустаяТаблицаУказателейПотоков_СоздаетТипизированныеКолонки` | без параметров | 282 | 1943–1955 | Пройден |
| B06 | `ДобавитьУказательПотока_СохраняетПотокИМетку` | без параметров | 314 | 1984–1990 | Пройден |
| B06 | `ЗафиксироватьУказатели_ОбрабатываетПустуюТаблицу` | без параметров | 333 | 2000–2014 | Пройден |
| B06 | `ЗафиксироватьУказатели_МеняетТолькоНулевойСтатусСвоегоПотока` [1] | `-1, -1` | 351 | 2000–2014 | Пройден |
| B06 | `ЗафиксироватьУказатели_МеняетТолькоНулевойСтатусСвоегоПотока` [2] | `0, 1` | 351 | 2000–2014 | Пройден |
| B06 | `ЗафиксироватьУказатели_МеняетТолькоНулевойСтатусСвоегоПотока` [3] | `1, 1` | 351 | 2000–2014 | Пройден |
| B06 | `ЗафиксироватьУказатели_СохраняетСтатусыБезЗавершенныхПотоков` | без параметров | 379 | 2000–2014 | Пройден |
| G04 | `РассчитатьУказательУдаления_УчитываетСрокИПозициюЛогов` [1] | `-1, Неопределено, Неопределено` | 404 | 4256–4278 | Пройден |
| G04 | `РассчитатьУказательУдаления_УчитываетСрокИПозициюЛогов` [2] | `-1, 100, Неопределено` | 404 | 4256–4278 | Пройден |
| G04 | `РассчитатьУказательУдаления_УчитываетСрокИПозициюЛогов` [3] | `-2, Неопределено, Неопределено` | 404 | 4256–4278 | Пройден |
| G04 | `РассчитатьУказательУдаления_УчитываетСрокИПозициюЛогов` [4] | `-2, 100, 100` | 404 | 4256–4278 | Пройден |
| G04 | `РассчитатьУказательУдаления_УчитываетСрокИПозициюЛогов` [5] | `0, Неопределено, 172800000` | 404 | 4256–4278 | Пройден |
| G04 | `РассчитатьУказательУдаления_УчитываетСрокИПозициюЛогов` [6] | `1, Неопределено, 86400000` | 404 | 4256–4278 | Пройден |
| G04 | `РассчитатьУказательУдаления_УчитываетСрокИПозициюЛогов` [7] | `0.5, Неопределено, 129600000` | 404 | 4256–4278 | Пройден |
| G04 | `РассчитатьУказательУдаления_УчитываетСрокИПозициюЛогов` [8] | `1, 100, 100` | 404 | 4256–4278 | Пройден |
| G04 | `РассчитатьУказательУдаления_УчитываетСрокИПозициюЛогов` [9] | `1, 86400000, 86400000` | 404 | 4256–4278 | Пройден |
| G04 | `РассчитатьУказательУдаления_УчитываетСрокИПозициюЛогов` [10] | `1, 172800000, 86400000` | 404 | 4256–4278 | Пройден |
| G04 | `РассчитатьУказательУдаления_ОтклоняетНекорректныйСрок` | без параметров | 417 | 4256–4278 | Пройден |
| I08 | `ХэшСуммаТела_СовпадаетСКонтрольнойСуммой` [1] | `"", "e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855"` | 440 | 5251–5258 | Пройден |
| I08 | `ХэшСуммаТела_СовпадаетСКонтрольнойСуммой` [2] | `"abc", "ba7816bf8f01cfea414140de5dae2223b00361a396177a9cb410ff61f20015ad"` | 440 | 5251–5258 | Пройден |
| I08 | `ХэшСуммаТела_ПовторяетсяИМеняетсяПриИзмененииТела` | без параметров | 453 | 5251–5258 | Пройден |
| A12 | `ПредставленияИдентификатораОпераций_СопоставляетДевятьИдентификаторов` | без параметров | 150 | 1072–1098; 1367–1441 | Пройден |
| A12 | `КлючиСлужебныхОпераций_ВозвращаетФиксированныйСписок` | без параметров | 185 | 1104–1114; 1157–1203; 1323–1338; 2180–2192 | Пройден |

## Проверки и незавершённая работа

- `write_module_source`: синтаксическая проверка пройдена для каждой записи.
- `get_project_errors` после каждого изменения: нет диагностик двух затронутых общих модулей.
- `revalidate_objects` для обоих модулей: оба объекта найдены и проверены; последующая диагностика без замечаний. Последующее добавление A12 также проверено через `get_project_errors`.
- Структурная проверка текста, полученного через EDT: все 15 зарегистрированных процедур существуют; строк длиннее 120 символов нет. Зарегистрировано 33 сценария с учетом параметров.
- Пользователь разрешил обновление тестовой базы и запуск тестов. `update_database` выполнен через `unit-edt` для приложения `Адаптер Kafka - UNIT` (`c408ec97-28c7-46ec-8598-02aff4b31e67`): `success=true`, `action=updated`, переход `INCREMENTAL_UPDATE_REQUIRED` → `UPDATED`. Обновление инкрементальное, при внешних изменениях базы задана отмена.
- Первая попытка запуска после обновления была отклонена с "Project is building". После нового запроса пользователя готовность всех проектов подтверждена как ready; тестовая база повторно инкрементально обновлена.
- Первый завершённый прогон: 33 сценария, 32 пройдены, 1 ошибка теста. Проверка индекса использовала неподдерживаемое обращение к коллекции индексов через [0] и вызов Количество() у индекса. По документации unit-edt коллекция ИндексыКоллекции поддерживает перебор, а ИндексКоллекции перебирает строки. Утверждение исправлено: проверяются единственный индекс, единственная колонка и её имя через перебор.
- После исправления выполнены revalidate_objects и get_project_errors: замечаний нет.
- Итоговый узкий прогон run_yaxunit_tests: **Total 33, Passed 33, Failed 0, Errors 0, Skipped 0; PASSED**. Конфигурация запуска: base Тонкий клиент; модуль: ОМ_кфкФоновыеОперацииСлужебный; updateScope: extension:unit; externalInfobaseChanges: cancel.
- Отчёт runner: C:/EDT/tmp/edt-mcp-yaxunit/base_______________cf5560c21266_ed87d17ded98398ca8b3f2cec7c97f87978c4417/report.md. Это временный путь runner; таблица выше сохраняет результаты текущего прогона.
- Подтверждены вызовы исходных неэкспортных методов через обёртки, контрольные суммы пустой строки и abc для строк и байтов UTF-8. Успешный прогон не является замером покрытия.
- Помимо перечисленных в матрице сценариев и добавлений B08/B11 ниже, остальные сценарии A–I и воспроизводящие R01–R10 **не реализованы**. Цель 100% не достигнута; знаменатели исполняемых строк и ветвей не измерены. Процент покрытия не вычислялся.

## Исправление областей

По std455 и образцу ОМ_кфкОбработкаСобытийСлужебный регистрация и все предметные области тестов объединены в СлужебныйПрограммныйИнтерфейс. Область СлужебныеПроцедурыИФункции удалена: внутренних помощников в модуле нет. Структура подтверждена get_module_structure; revalidate_objects и get_project_errors — без замечаний. После изменения повторный прогон: 33 из 33, ошибок и пропусков нет.

## Продолжение: B08 и расчетная часть B11

Добавлена обёртка ют_ОбновитьУказательНачалаВыборки, вызывающая исходный метод base и сохраняющая выходной параметр позиции. Добавлено пять тестовых процедур с 14 сценариями. Регистрация и тесты находятся в СлужебныйПрограммныйИнтерфейс; внутренние помощники подготовки и проверки позиции — в отдельной СлужебныеПроцедурыИФункции с предметными подразделами.

| ID | Тест | Параметры / утверждение | Результат |
|---|---|---|---|
| B08 | ОбновитьУказательНачалаВыборки_УчитываетПустуюВыборку | Граница 500000 / 1000000 / 2000000 при старой позиции 1000000; сохраняется максимум, пустая таблица остается пустой | 3/3 |
| B08 | ОбновитьУказательНачалаВыборки_НеПерескакиваетНезавершеннуюПорцию | Все 8 комбинаций статусов 0/1 трех порций; входные метки 6000000, 2000000, 4000000 не отсортированы. Ожидаемые позиции 1000000 / 1100000 / 3100000 / 5100000; проверяются точный оставшийся суффикс и отсутствие записи без продвижения | 8/8 |
| B08 | ОбновитьУказательНачалаВыборки_УдаляетИсчезнувшийПоток | Средний незавершенный поток отсутствует в списке; его указатель удаляется, поздняя завершенная порция остается, позиция 1100000 | 1/1 |
| B08 | ОбновитьУказательНачалаВыборки_УчитываетРавныеМетки | Три завершенные порции с меткой 4000000; все учтены и удалены, позиция 3100000 | 1/1 |
| B11, B08 | ОбновитьУказательНачалаВыборки_ПродолжаетПослеЗавершенияОстатка | Статусы 1/0/1 дают позицию 1100000; после фиксации среднего потока повторный вызов дает 5100000 и пустую таблицу | 1/1 |

Исходные методы: ОбновитьУказательНачалаВыборки (2056–2097), ОбновитьПозицию (1898–1900), УказательПогрешностьСмещения и ЗафиксироватьУказатели. Строки — ориентиры исходника, не замер покрытия. Запись регистра кфкПозицииОпераций проверяется через ОжидаетЧтоТаблицаБазы, включая UUID, раздел, его представление и метку; отсутствие записи также проверяется специализированным утверждением.

Проверки после изменения: write_module_source — синтаксис пройден; revalidate_objects и get_project_errors обоих модулей — без замечаний. Узкий run_yaxunit_tests с модулем ОМ_кфкФоновыеОперацииСлужебный и единственным тегом ОбновитьУказательНачалаВыборки: **Total 14, Passed 14, Failed 0, Errors 0, Skipped 0**. Общий прогон всех 47 после добавления не выполнялся.
Отчет: C:/EDT/tmp/edt-mcp-yaxunit/base_______________30e0cafc1cb1_9288a278fec4c367a97cdadce9e6523808deb9fe/report.md.

B08 пока не закрыт полностью: смешанные статусы при равных метках, предварительный статус -1 и проверка сохранности контрольной записи другой операции требуют дополнения. B11 реализован только в части расчета позиции; реальный остаток сообщений очереди после повторного старта еще не проверен.

## Точка остановки после B08

Подготовка B07 (ОбновитьСтатусУказателей) остановлена до изменения его тестов. code-index.grep_code для alias kfk-unit, root C:/EDT/projects/kafka/tests/unit/unit, scope src/CommonModules/РС_кфк*/Module.bsl вернул status=error: «базовый индекс обновлён, но слой extras не пересобран — граф вызовов и связи данных отстают, см. журнал демона». Прежний health=ready для этого alias больше не считается актуальным. По Evidence and Failure Gates зависимая работа остановлена без обхода через другой MCP или файловую систему.

Для продолжения требуется восстановление полноты индекса kfk-unit и новый запрос пользователя. Далее: подтвердить ready, завершить выбор фикстуры независимого регистра кфкОчередьПотоков и реализовать B07. В v8std проверен КонструкторОбъекта(Менеджер), но поддержка независимого регистра им не установлена; КонструкторДвижений относится к документу-регистратору и для этого регистра не подходит. Не считать неподтвержденный способ подготовки данных готовой фикстурой.

## Продолжение после восстановления индекса

10.09.2026 новый запрос пользователя: code-index.health подтвердил daemon healthy, endpoint_verified=true и ready для kfk-unit с корнем C:/EDT/projects/kafka/tests/unit/unit. Поиск и чтение известного теста успешны; после изменений индекс также вернул новый метод B07. Все четыре проекта unit-edt готовы. Живой hash base повторно получен: cfbe824c47a32eb0, 5262 строки.

Подтвержден публичный способ подготовки независимого регистра: ЮТест.Данные().КонструкторОбъекта(РегистрыСведений.кфкОчередьПотоков), Установить трех измерений и Записать. В установленном YAxUnit прослежена цепочка до создания набора, добавления строки и установки отборов по измерениям. Работоспособность подтверждена тестами. Новые записи изолированы транзакцией набора; ключи и UUID собственные.

| ID | Тест / параметры | Результат |
|---|---|---|
| B07 | ОбновитьСтатусУказателей_ФиксируетТолькоСвободныеПотоки: четыре сочетания занятости двух потоков, чужой UUID вне списка, две записи первого занятого потока, сохранность очереди и статусов -1/1 | 4/4 |
| B07 | ОбновитьСтатусУказателей_СохраняетОчередьБезУказателей | 1/1 |
| B07 | ОбновитьСтатусУказателей_СохраняетСтатусыБезПотоков | 1/1 |
| B07 | ОбновитьСтатусУказателей_ФиксируетВсеСвободныеПотоки | 1/1 |
| B08 | ОбновитьУказательНачалаВыборки_НеПерескакиваетНезавершеннуюПорцию: добавлены -1/1/1, 1/-1/1, 1/1/-1; заранее заданный -1 остается и останавливает продвижение | 3/3 |
| B08 | ОбновитьУказательНачалаВыборки_СохраняетНезавершеннуюПриРавныхМетках: незавершенная порция в каждой из трех входных позиций; она сохранена, указатель не выходит за безопасную границу; после завершения учтен остаток | 3/3 |
| B08 | ОбновитьУказательНачалаВыборки_СохраняетПозициюДругойОперации: другая операция в том же разделе сохраняется в обеих ветвях записи позиции | 2/2 |

В unit добавлена восьмая минимальная обертка ют_ОбновитьСтатусУказателей. Алгоритмы продукта не изменены и не скопированы. Тесты и регистрация находятся в СлужебныйПрограммныйИнтерфейс, внутренние помощники — в СлужебныеПроцедурыИФункции. После каждого изменения синтаксическая проверка и focused get_project_errors двух модулей успешны, замечаний нет.

Прогоны: B07 — **7/7**; B08 — **22/22**; весь целевой модуль — **62/62, Failed 0, Errors 0, Skipped 0**. База предварительно успешно обновлена через update_database; run_yaxunit_tests использовал updateBeforeLaunch=true, updateScope=extension:unit, externalInfobaseChanges=cancel. Общий отчет: C:/EDT/tmp/edt-mcp-yaxunit/base_______________cf5560c21266_ed87d17ded98398ca8b3f2cec7c97f87978c4417/report.md.

Это промежуточная фиксация результатов до добавления следующего сценария B11. Замер покрытия не выполнен, 100% не заявляется.

## B11: реальная очередь и история первоначальной остановки

Добавлен и зарегистрирован ПовторныйСтарт_ОбрабатываетОстатокБезПотери, тег ПовторныйСтарт. Подготовка использует публичный конструктор YAxUnit для собственного продюсера, двух исходящих сообщений, назначений потоков и начальной позиции. Даты регистрации согласованы с миллисекундными метками. Проверяются исходные выборки и обработчик сериализации, обновление статусов указателей, чтение сохраненной позиции и выбор остатка. Поздняя порция обрабатывается первой; после освобождения назначения ранняя порция должна выбираться повторно и обрабатываться ровно один раз. Реальные ФЗ не нужны, весь сценарий изолирован транзакцией.

Произвольный обработчик СериализоватьСообщениеПовторногоСтарта возвращает сообщение через кфкИнтеграция.ПараметрыСообщения; он является экспортной точкой вызова по имени, поэтому находится в СлужебныйПрограммныйИнтерфейс. Внутренние помощники подготовки и утверждений находятся отдельно в СлужебныеПроцедурыИФункции. Это тестовая внешняя граница произвольного обработчика; алгоритмы продукта не копируются и не заменяются.

В заимствованном модуле теперь 12 оберток. Последние четыре предоставляют исходные выборки диспетчера и потока, обработчик сериализации с исходными наборами очередей и очистку назначения одного потока. После каждого изменения focused get_project_errors обоих модулей: 0 замечаний, objectsNotFound и objectsUnsupported пусты; синтаксическая проверка успешна.

Узкий прогон B11: **Total 1, Passed 0, Failed 1, Errors 0, Skipped 0**. Первая проверка позднего сообщения не нашла ожидаемую запись со статусом Обработано и КоличествоОбработок=1 (ключ запуска 8ebbca6d-27a0-427c-b50a-4b23eea08ee7, ДатаРегистрацииМС=4000000). Проверки самого повторного старта после этого утверждения не выполнены. Нельзя считать это доказанным дефектом продукта или менять ожидание ради зеленого результата.

Отчет неуспешного запуска: C:/EDT/tmp/edt-mcp-yaxunit/base_______________1ce2227fde6b_a2b773e202d56a41e26c07528965652b585a3dac/report.md. Общего прогона 63 после добавления B11 не было; последний успешный общий прогон относится к предыдущим 62 сценариям.

Для классификации выполнен узкий debug-запуск. unit-edt.wait_for_break подтвердил остановку в проекте base, CommonModules/кфкФоновыеОперацииСлужебный/Module.bsl:2408, непосредственно после формирования РезультатСериализации. Live hash base повторно подтвержден как cfbe824c47a32eb0. get_variables(frameRef=8858, expandPath="РезультатСериализации") вернул success=false, isError=true: **expandPath not found: РезультатСериализации**. Документированный формат expandPath — имя переменной и далее путь через точки. Из этого ответа нельзя заключить, что переменная отсутствует в исходнике или что сериализация завершилась конкретной ошибкой. Зависимый анализ остановлен по Evidence and Failure Gates; обход через evaluate_expression, shell или другой источник не выполнялся.

Очистка отладочного состояния: собственная точка останова 1227 удалена (removed=true), поток 8857 продолжен (resumed=true), чтобы тест мог штатно завершиться. Итоговый отчет debug-сеанса и завершение отката отдельно не проверены. Каталог debug-run: C:/EDT/tmp/edt-mcp-yaxunit-debug/base-1789036810469-48.

Следующий шаг после восстановления чтения переменных и нового запроса пользователя: получить фактические Статус и ТекстЖурнала из РезультатСериализации исходного обработчика, классифицировать ошибку B11 и исправить только тестовую сторону, если причина в ней. Затем узкий B11, общий прогон целевого модуля и продолжение остальных групп A–I/R01–R10. При подтвержденном дефекте продукта его исправление остается вне разрешенного scope. Полный план и цель 100% не выполнены.

## Повторная отладка B11: блокировка снята

По новому запросу пользователя повторен debug-запуск с остановкой base:2408. get_variables без expandPath сначала вернул служебный элемент «(вычисление локальных переменных)», затем повторное чтение того же кадра — девять переменных, включая РезультатСериализации. Раскрытие структуры также потребовало повторного чтения: сначала возвращались элементы с типом <?>, затем полноценные КлючИЗначение. Это отложенная загрузка значений отладчика; прежний вывод о необходимости восстановления инструмента был преждевременным.

Фактический Статус — ОшибкаОбработки. ТекстЖурнала содержит ошибку ПрочитатьXML на вызове ЗначениеИзХранилища (base:2448), причина: «Ошибка разбора XML: - [1,1] ... Document is empty». Диагностика классифицирована как ошибка тестовой фикстуры: она создавала Новый ХранилищеЗначения(Продюсер), тогда как контракт адаптера хранит XML-представление значения. В НовоеСообщениеПовторногоСтарта заменена только подготовка поля: кфкОбщегоНазначенияСлужебный.ЗначениеВХранилище(Продюсер). Продуктовый код и ожидания теста не изменены.

Своя точка останова 1228 удалена, debug-поток продолжен. Синтаксическая проверка изменения успешна; focused get_project_errors обоих модулей — 0 замечаний. Узкий B11: **1/1**. Затем весь целевой модуль: **Total 63, Passed 63, Failed 0, Errors 0, Skipped 0**. Отчеты обычных запусков по прежним путям перезаписаны успешными результатами.

Текущий остаток: продолжить остальные группы A–I и R01–R10, получить инструментальный замер исходного модуля. Прежняя блокировка больше не действует; полный план и цель 100% остаются незавершенными.

## A05/A06 и воспроизведение E01/R01

По очередному запросу пользователя готовность подтверждена: code-index daemon healthy, kfk-unit и kfk ready, все четыре проекта unit-edt ready. Сохранены результаты и фикстуры предыдущих 63 сценариев.

| ID | Тест / параметры | Результат |
|---|---|---|
| A05 | ОбновитьФактЗапуска_МеняетТолькоФлагНаправления: продюсер/консьюмер/неизвестный тип × транспорт/обработка × исходные флаги Ложь/Истина; проверяются все четыре поля и неизменность состава | 12/12 |
| A06 | СписокОперацийОбмена_ВозвращаетОбеОперацииНезависимоОтФильтра: два направления × Неопределено/Истина/Ложь; точные имена, представления, порядок и транспортные пометки | 6/6 |
| A06 | СписокОперацийОбмена_ОтклоняетНеизвестноеНаправление: неизвестный тип × три значения фильтра; характеризация наличия исключения без закрепления его текста | 3/3 |
| E01/R01 | ИнициализироватьКэшРаспределенияЧтения_СоздаетКэшПриПервомВызове: пустой массив консьюмеров и Неопределено вместо предыдущего кэша; ожидается созданная структура | Падение: возвращено Неопределено |

A05 пока проверяет непосредственно флаги; отсутствие отметки при недоступных настройках диспетчера отдельно не проверено. A06 фиксирует текущее отсутствие фильтрации. Требование будущей фильтрации остается отдельным решением по контракту; тест не выдумывает ее существование.

В unit теперь 15 минимальных оберток; новые: ют_ОбновитьФактЗапуска, ют_СписокОперацийОбмена, ют_ИнициализироватьКэшРаспределенияЧтения. Тесты и регистрация остались в СлужебныйПрограммныйИнтерфейс; новый внутренний конструктор НовыеФлагиЗапуска — в СлужебныеПроцедурыИФункции. Все изменения исходников выполнены через unit-edt с expectedHash и синтаксической проверкой. После каждой записи focused get_project_errors двух модулей вернул 0 замечаний, objectsNotFound и objectsUnsupported пусты.

Первый узкий прогон A05/A06 выявил ошибку теста: у ВыбрасываетИсключение обязателен параметр ожидаемого текста. Сигнатура проверена по v8std; для проверки любого исключения явно передана пустая строка. Повторный узкий прогон: **Total 21, Passed 21, Failed 0, Errors 0, Skipped 0**. Отчет: C:/EDT/tmp/edt-mcp-yaxunit/base_______________e14925b53fa9_0971879c50bc81ff911dba7d61faf143c9a12e99/report.md.

E01/R01: live base:3045–3064, hash cfbe824c47a32eb0. При КЭШ = Неопределено исходная функция сразу выполняет Возврат КЭШ. Тест прямым вызовом оригинальной функции через обертку подтвердил Неопределено; данные БД, сеть, подмена кэша и фоновые задания в воспроизведении не используются. Это отличие от прежней ошибки фикстуры B11: здесь результат непосредственно определяется условием продукта. По исходнику вызывающего метода ТаблицаРаспределенияЧтенияКонсьюмеров (2983–3022) результат присваивается КЭШ и сразу читается КЭШ.ОписаниеКонсьюмеров. Отдельный запуск вызывающего метода пока не выполнен. Потеря истории при повторном вызове остается отдельной непроверенной частью R01.

Узкий E01: **Total 1, Passed 0, Failed 1, Errors 0, Skipped 0**. Сообщение: «Ожидали, что проверяемое значение <Пустое значение, Тип: Не определено> не является неопределено, но это не так». Утверждение: строка 608 тестового модуля. Отчет: C:/EDT/tmp/edt-mcp-yaxunit/base_______________031639dd3148_4ea16dc098d1e94abc98e80311a28d2aae3bb07f/report.md.

Общий прогон текущего модуля: **Total 85, Passed 84, Failed 1, Errors 0, Skipped 0**. Единственное падение — тот же E01/R01. Отчет: C:/EDT/tmp/edt-mcp-yaxunit/base_______________cf5560c21266_ed87d17ded98398ca8b3f2cec7c97f87978c4417/report.md. Общий отчет по этому пути теперь содержит падение, а не прежний результат 63/63.

Историческая блокировка приемки этого прогона: продуктовый метод принадлежал adapter/adapter и собранной base, изменение которых запрещено scope. Обертку не исправляли вместо оригинала; тест не пропущен, ожидание не ослаблено. В следующем запуске пользовательское исправление подтверждено и это падение снято; новое разрешение на исправление данного условия больше не требуется.

Остальные группы плана, инструментальный замер и часть R01 о повторном использовании кэша остаются открытыми. Независимые тесты можно продолжать отдельно от исправления R01; полный план и 100% покрытия не достигнуты.

## Проверка пользовательского исправления и новая блокировка

10.09.2026, продолжение по приложенному запросу пользователя. unit-edt работает на порту 8768, EDT 2026.2.0.289, MCP 2.16.1; проекты base, examples, unit и yaxunit имеют state=ready. Code-index daemon healthy, точные alias kfk-unit и kfk ready. Эти проверки относятся к данной сессии и не заменяют проверку готовности после перезапуска.

В unit-edt/base прочитана ИнициализироватьКэшРаспределенияЧтения: hash `6fdf7dc94e2cfc19`, фрагмент с документацией 3024–3064, всего 5262 строки. Условие раннего возврата теперь `Если КЭШ <> Неопределено Тогда`. При первом вызове создаются структура, соответствия и таблица истории; при готовом кэше исходник возвращает переданный объект. Последнее пока подтверждено только исходником, отдельный тест сохранения истории не добавлен. Canonical checkout отдельно не сверялся; ручная пересборка и правка base не требовались.

get_applications для «Адаптер Kafka - UNIT», applicationId `c408ec97-28c7-46ec-8598-02aff4b31e67`, показал INCREMENTAL_UPDATE_REQUIRED. На основании ранее выданного разрешения выполнены preview и apply update_database с fullUpdate=false, externalInfobaseChanges=cancel. Ответ: success=true, action=updated, stateAfter=UPDATED. Исходники продукта агентом не изменялись.

| Прогон | Total | Passed | Failed | Errors | Skipped |
|---|---:|---:|---:|---:|---:|
| E01, тег ИнициализироватьКэшРаспределенияЧтения | 1 | 1 | 0 | 0 | 0 |
| Весь ОМ_кфкФоновыеОперацииСлужебный | 85 | 85 | 0 | 0 | 0 |

Оба запуска: base Тонкий клиент, updateBeforeLaunch=true, updateScope=extension:unit, externalInfobaseChanges=cancel, timeout=45. Отчёты по прежним путям `base_______________031639dd3148_4ea16dc098d1e94abc98e80311a28d2aae3bb07f/report.md` и `base_______________cf5560c21266_ed87d17ded98398ca8b3f2cec7c97f87978c4417/report.md` в `C:/EDT/tmp/edt-mcp-yaxunit/` теперь содержат успешные результаты. Прежние 84/85 больше не являются последним результатом.

Перед добавлением теста повторного вызова выполнен get_project_errors с точными objectFqns CommonModule.ОМ_кфкФоновыеОперацииСлужебный и CommonModule.кфкФоновыеОперацииСлужебный в unit. Первая выдача достигла лимита 100; документированное увеличение limit до 1000 дало полный ответ из 557 замечаний, objectsNotFound и objectsUnsupported пусты. Изменений src в этой сессии до диагностики и после неё не было: все замечания предшествуют предлагаемому изменению.

| Код EDT | Количество |
|---|---:|
| dynamic-access-method-not-found | 498 |
| property-return-type | 2 |
| variable-value-type | 12 |
| doc-comment-type | 34 |
| typed-value-adding-to-untyped-collection | 3 |
| constructor-function-return-section | 4 |
| doc-comment-return-section-type | 1 |
| doc-comment-export-function-return-section | 1 |
| function-return-value-type | 1 |
| invocation-parameter-type-intersect | 1 |

В существующих пользовательских изменениях `.settings/com.e1c.v8codestyle.prefs` commonChecks и standardChecks переключены с false на true. Настройки сохранены; проверки не отключались и замечания не подавлялись. Это объясняет изменение набора включённых проверок, но само по себе не классифицирует замечания.

Примеры, затрагивающие ближайшее изменение: dynamic-access-method-not-found — «Метод \"Тег\" не найден в исходном объекте», тестовый модуль:263–265, 268; «Метод \"ДобавитьСерверныйТест\" не найден в исходном объекте», :266. В заимствованном модуле:227 отмечены doc-comment-type для описания кэша; :247 — constructor-function-return-section. Успешный runtime не является основанием объявить весь набор ложноположительным.

Для проверки фактического разрешения вызова на строке 263, колонке 5 вызван unit-edt.get_symbol_info. Вместо типа, сигнатуры или структурного описания он вернул `com.\\_1c.g5.v8.dt.bsl.ui.hover.BslAnnotationWithQuickFixesHover$BslAnnotationInfo@5bed6819`. Требуемые семантические данные отсутствуют. Классификация причины неразрешённых вызовов остаётся indeterminate; непригодный ответ семантического инструмента — инфраструктурное препятствие. По Evidence and Failure Gates дальнейшие инженерные изменения остановлены до восстановления и нового запроса пользователя; обход другим источником и подавление диагностик не выполнялись.

Остаток на момент этой остановки: восстановить пригодный ответ семантической проверки unit-edt, классифицировать замечания в затрагиваемом коде, добавить тест сохранения кэша/истории при повторном вызове, продолжить остальные группы и измерить покрытие. Полный план не завершён. В той сессии изменены только план и данный документ; исходники unit, adapter, base, examples и yaxunit не изменялись агентом.

## Продолжение: E01, E04, E05 и воспроизведение R04

После нового запроса пользователя list_projects подтвердил ready для четырёх проектов. Свежий get_project_errors двух модулей вернул 0 замечаний; перед первой записью результат повторился. После изменений и пересчёта проверок замечания в прежнем коде появились вновь. Нулевой снимок не следует считать доказательством устранения всех исторических замечаний. Ошибки dynamic-access-method-not-found больше не наблюдаются, прежняя блокировка на них не действует.

Через unit-edt добавлены четыре экспортные тестовые процедуры, один внутренний конструктор и три минимальные экспортные обёртки. Всего теперь 18 обёрток. Продуктовые алгоритмы не менялись и не копировались. Тесты и регистрация сохранены в СлужебныйПрограммныйИнтерфейс, новый конструктор НовыеПараметрыОчисткиВходящихПоЛогам — в СлужебныеПроцедурыИФункции. Callback СериализоватьСообщениеПовторногоСтарта остался в служебном программном интерфейсе. Финальная структура проверена через get_module_structure: 43 процедуры и 7 функций, 1560 строк тестового модуля.

| ID | Тест | Параметры / наблюдаемый результат | Строка теста | Результат |
|---|---|---|---:|---|
| E01/R01 | ИнициализироватьКэшРаспределенияЧтения_СохраняетКэшИИсторию | Возврат того же объекта, сохранение ссылок на описания и коннекторы, записи числа партиций, одной строки истории и всех её полей | 656 | 1/1; вместе с первым созданием 2/2 |
| E04 | СтатистикаРаспределенияЧтения_УчитываетМаксимумИстории | Нет замеров; один размер 12; пары 12/6, 6/12, 12/12. Максимумы 0/12, три партиции, средний размер 0/4 | 691 | 5/5 |
| E05 | РаспределитьКонсьюмеровПоПотокам_СоблюдаетПорогИЛимиты | Средние размеры 0, 99, 100, 101, 149, 150, 500 при пороге 100; ограничения 2/4 партиции и 1/4 потока. Проверяются количество назначений, допустимые номера, максимальный номер, адресат и удаление СредРазмер из результата | 735 | 9/9 |
| G06/R04 | УказателиУдаленияВходящих_ИспользуетПозициюВходящихЛогов | Разные позиции входящих и исходящих логов; для Обработано ожидается входящая граница | 986 | 0/1; подтверждён дефект продукта |

Обёртки: ют_СтатистикаРаспределенияЧтения, ют_РаспределитьКонсьюмеровПоПотокам, ют_УказателиУдаленияВходящих. E01/E04/E05 используют локальные коллекции и пустую ссылку консьюмера как ключ: обращения к брокерам, фоновые задания и запись консьюмера не нужны. R04 задаёт две позиции регистра через ЮТест.Данные().КонструкторОбъекта; набор ВТранзакции изолирует также возможную замену существующих записей с фиксированными ИД. Прямой вызов расчётной функции не удаляет сообщения и не отправляет логи.

Перед узким E01 выполнены preview/apply инкрементального update_database, ответ UPDATED. Запуск E01 вернул Pending, jobId `06e72fbc-54b0-4503-8187-dff0b84fc621`; последующий get_job_status подтвердил done и 2/2. Подготовка повторяла prep:db-update, затем штатно перешла к spawn/run; пользователь сообщил, что диалога в EDT нет. Повторный запуск вместо polling не создавался.

| Проверка | Total | Passed | Failed | Errors | Skipped |
|---|---:|---:|---:|---:|---:|
| E01 | 2 | 2 | 0 | 0 | 0 |
| E04 | 5 | 5 | 0 | 0 | 0 |
| E05 | 9 | 9 | 0 | 0 | 0 |
| R04 | 1 | 0 | 1 | 0 | 0 |
| Общий прогон целевого модуля | 101 | 100 | 1 | 0 | 0 |

Все прогоны: base Тонкий клиент, modules=[ОМ_кфкФоновыеОперацииСлужебный], updateBeforeLaunch=true, updateScope=extension:unit, externalInfobaseChanges=cancel, timeout=45. Узкие прогоны использовали один тег проверяемой операции. Общий отчёт `C:/EDT/tmp/edt-mcp-yaxunit/base_______________cf5560c21266_ed87d17ded98398ca8b3f2cec7c97f87978c4417/report.md` перезаписан результатом 100/101. Каталоги узких отчётов в том же корне: E04 — `base_______________4ad0f31d33b8_6a18c904f76af39da342128dc430f4d1e3ade623`; E05 — `base_______________f718b0cd815e_5e5bc35b3348bb57b3a246533809d1a7e32a2a31`; R04 — `base_______________135db4b9778b_5f35eb14d6af09d3a4ee5f316d3605c2f4a98361`.

### Классификация R04

Актуальный live base сохраняет hash `6fdf7dc94e2cfc19`. УказателиУдаленияВходящих, фрагмент 4192–4245, при включённом учёте логирования вызывает ПолучитьПозицию с ИДОперации_ВыгрузкаЛоговИсходящихСообщений. В тесте исходящая позиция равна 1000000, входящая — 2000000. Для обработанных сообщений задан срок -2, остальные сроки отключены (-1), поэтому результат не зависит от текущего времени или позиции удаления дублей.

Узкий и общий прогоны падают на тестовом модуле:1011: «Входящие сообщения ограничиваются позицией входящих логов: ожидали, что проверяемое значение 1 000 000 равно 2 000 000, но это не так». Результат содержит единственную границу, совпадающую с исходящей позицией. Классификация: дефект выбора направления в продукте, подтверждённый исходником и прямым вызовом. Исправление adapter/base не разрешено; ожидание не ослаблялось, тест не исключён. Это блокирует критерий полного плана «все R-пункты имеют решение; 0 падений».

### Диагностика и ограничения приёмки

Каждая запись выполнена с expectedHash и успешной синтаксической проверкой, после каждой прочитан get_project_errors двух точных объектов. Финальный revalidate_objects подтвердил objectsFound=2; следующая диагностика вернула 70 замечаний без объектов notFound/unsupported и без обрезки результата:

| Код | Количество | Классификация |
|---|---:|---|
| doc-comment-type | 50 | 34 в прежнем коде; 16 новых замечаний к именам полей документирующих комментариев |
| variable-value-type | 7 | Прежний код |
| doc-comment-return-section-type | 1 | Прежний код |
| doc-comment-export-function-return-section | 1 | Прежний код |
| function-return-value-type | 1 | Прежний код |
| invocation-parameter-type-intersect | 1 | Прежний код |
| constructor-function-return-section | 4 | Прежний код |
| property-return-type | 2 | Прежний код |
| typed-value-adding-to-untyped-collection | 3 | Прежний код |

54 замечания сопоставлены с историческим списком 557 по коду, сообщению, объекту и сдвигу строки. Их первопричины не устранены и они не объявляются ложноположительными. По правилу corporate:work:bsl-change-policy о незатронутом legacy-коде они не использовались как причина массового изменения старых методов. Из новых замечаний variable-value-type у результата ют_СтатистикаРаспределенияЧтения устранено явным типом ТаблицаЗначений; module-unused-method у нового конструктора исчезло после добавления вызывающего теста.

16 новых doc-comment-type классифицированы как ложноположительные к описанию полей: в заимствованном модуле:250 — Консьюмер, РазмерОчереди, Партиций, СредРазмер; :268 — Консьюмер, НомерПотока; :287 — пять полей ПараметрыОчистки; в тестовом модуле:1254 — те же пять полей возвращаемой структуры конструктора. Сообщение имеет вид «Неизвестный тип [имя поля] указан в документирующем комментарии». Доказательство классификации: std641, пункты 1.4–1.5, задаёт именно описание полей через `* Имя - Тип`; эти имена не объявлены типами, справа указаны Число, Булево и СправочникСсылка.кфкКонсьюмеры. Live constructor base НоваяТаблицаСтатистикиРаспределенияЧтения и новый конструктор unit подтверждают состав и типы; остальные контракты сопоставлены с прочитанными телами исходных функций. Диагностики не подавлялись, формат не менялся ради обхода проверки. get_check_description для doc-comment-type не предоставил справку (это документированная необязательная возможность); нормативная оценка основана на уже загруженном std641.

Структура нового кода, имена, экспорт, передача параметров по значению, типы и вложенность fluent-цепочек проверены по ранее загруженным общим требованиям и YAxUnit authoring-baseline/test-module/naming/registration-and-parameters/assertions/test-data/data-isolation. Чистая диагностика всего модуля не заявляется.

### Остаток

На момент предыдущего этапа E04 не проверял отсечение истории и независимость консьюмеров; E05 — пустую статистику, индекс, совместное размещение и негативные лимиты. R04 воспроизведён только для позиции логирования. По последующему поручению пользователя реализация остальных тестов продолжается независимо от R04.

## Продолжение по поручению «добавь все тесты по плану»

Промежуточный общий прогон: **177 сценариев, 176 passed, 1 failed (прежний R04), 0 errors, 0 skipped**. Добавленные 76 сценариев проходят. Версия проверяемого base: `6fdf7dc94e2cfc19`. Продуктовые исходники не менялись. Отчёт: `C:/EDT/tmp/edt-mcp-yaxunit/base_______________cf5560c21266_ed87d17ded98398ca8b3f2cec7c97f87978c4417/report.md`.

| План | Добавлено | Проверки и focused результат |
| --- | ---: | --- |
| E05 | 5 | Пустая статистика, состав колонок и индекс; разные консьюмеры при сумме нагрузок 99/100/101, принудительное повторное использование при лимите 1. Группа 14/14. |
| E04 | 1 | Изоляция истории разных консьюмеров, отсутствие истории, игнорирование удалённого консьюмера. Группа 6/6. |
| C11 | 13 | Четыре типа RecordSet, каждое отсутствующее поле, оба отсутствуют, другой базовый тип, другие типы данных, выходной регистратор. 13/13. |
| H06–H08 | 42 | Префиксы и оба направления, ключ события и дробные метки, пустая/заданная дата, 15 поддерживаемых и 4 неподдерживаемых типа заголовка, фильтрация, пустые заголовки, условие добавления, пустое/пробельное/обычное тело. 42/42. Проверка даты подтверждает календарную часть; часовой пояс отдельно не проверен. |
| E07 | 11 | Наличие команды для своего/чужого UUID, посторонний ключ, 0/1/3 назначения с командой и без неё, неверный UUID назначения. 11/11. Восстановление глобального соответствия ещё не проверено. |
| E06 | 4 | Переходы отсутствует→отсутствует, отсутствует→назначен, назначен→отсутствует, назначен→назначен; локальный список, записи регистра, состав наборов добавления/удаления, сохранность чужого потока. 4/4. |

После каждой мутации запрашивалась focused диагностика обоих объектов. Кратковременный ответ `Project is building` потребовал дождаться завершения сборки и повторить диагностику до следующей мутации. Новые сообщения doc-comment-type о словах «ключ», «метка» и подобных исчезли после удаления избыточных однословных пояснений по std453 §5.2.1; объявления типов сохранены, подавление проверок не добавлялось. На последней проверке — прежние 70 замечаний. Ошибка использования утверждения YAxUnit в негативном E07 исправлена на `ЮТест.ОжидаетЧто(модуль).Метод(имя, массив)`; повторный прогон успешен.

План остаётся незавершённым. Не отмечены выполненными частичные E04–E07 и H06–H08; покрытие методов, строк и ветвей инструментально не измерялось. R04 не является причиной остановки остальных независимых тестов.

### Следующая контрольная точка: 250 сценариев

Общий прогон: **250 total, 248 passed, 2 failed, 0 errors, 0 skipped**. С начала поручения добавлено 149 сценариев. Общий отчёт обновлён по тому же пути. Помимо прежнего R04 воспроизведён дефект E05: после назначений 60→поток 1, 50→поток 2, 40→поток 2 нагрузка потоков равна 60 и 90. Следующее назначение 10 должно выбрать поток 1, но выбирает поток 2: алгоритм сортирует отдельные строки назначений по нагрузке, не суммируя их по потоку. Тест `РаспределитьКонсьюмеровПоПотокам_ВыбираетМенееЗагруженныйПоток(Истина)` сохранён падающим.

| План | Дополнительно | Проверки и focused результат |
| --- | ---: | --- |
| A08/A13 | 11 | Полное имя метода, ключ без диспетчера/со строкой/со ссылкой, представления, пять фиксированных констант. 11/11. Текущий реальный фоновый поток не покрыт. |
| I07 | 7 | Независимые комплекты очередей, типы и режим загрузки, очистка выбранного набора и снятие отбора с сохранностью второго, Неопределено. 7/7. |
| C13/F08/R08 | 10 | Обе очереди: одна строка, интервал 1999/2000/2001 мс, обновление всех строк только при >2000, сохранность ключей. Пустой набор ожидаемо вызывает существующую ошибку индекса; это характеристика текущего поведения, не исправление R08. 10/10. |
| I08 | 2 | SHA256 кириллицы «Привет» и байта 00; эталон независимо вычислен System.Security.Cryptography.SHA256. Полная группа 5/5. Нулевой байт создаётся внутри теста, поскольку NUL в строковом параметре регистрации нарушал передачу списка сценариев. |
| E05 | 5 | Нулевые размер пакета/число партиций/число потоков, наименее загруженный поток после трёх и четырёх назначений. Полная группа 18/19, одно продуктовое падение описано выше. |
| C09/F06 | 18 | Штатный вызов БСП с четырьмя аргументами, функция/процедура без результата, пустое/пробельное имя, комбинации исходного и выходного отказа, журнал, неотрицательная длительность, распространение исключения. 18/18. |
| G06 | 13 | Каждый срок исходящих и входящих, группировка связанных статусов, ограничение незавершённых позицией дублей 500 при позиции логов 1000, отключённые сроки и недоступная позиция логов. 13/13. Общая группировка разных сроков и выборки G07 ещё не реализованы. |
| C07 | 7 | Пустая/непустая строка, двоичные 00 01 02, структура→JSON, массив→JSON, Булево и Неопределено как неподдерживаемое тело; статус, ключ, заголовки, тип и содержимое хранилищ. 7/7. |

Итоговая сверка диагностик выполнялась по объекту, методу и строке, а не только совпадению текста сообщения. Найденные в новых методах девять `typed-value-adding-to-untyped-collection` устранены явными типами смешанных массивов аргументов; два замечания описания результата `ют_НоваяОчередь` устранены списком альтернативных типов по std453 §5.3. Устранены неверная ссылка на приватный конструктор из другого модуля, неопределённый тип параметра, недекларированный Неопределено, `statement-type-change` и тип элементов массива C07. Временные `module-unused-method` исчезли после регистрации вызывающих тестов.

Последний focused результат: **96 замечаний** = прежние 70 + 26 `doc-comment-type` к полям новых структур. Новые сообщения относятся к полям Очередь/ОчередьПотока в четырёх интерфейсах I07 (8), шести полям параметров исходящей очистки (6), трём полям свойств тестового обработчика (3) и девяти полям конструктора очистки (9). Имена полей ошибочно трактуются как типы; расположение `* Имя - Тип` соответствует std641, а реальные структуры и наборы подтверждены исходными конструкторами и успешными тестами. Подавлений диагностик нет. 20 прежних сообщений других кодов не исправлялись массово; их количество совпадает с исходной базой 70.

### Контрольная точка: 313 сценариев

Общий прогон: **313 total, 304 passed, 9 failed, 0 errors, 0 skipped**. С начала поручения добавлено 212 сценариев; после точки 250 — ещё 63. Проверяется прежний base `6fdf7dc94e2cfc19`, продукт не изменялся. Общий отчёт: `C:/EDT/tmp/edt-mcp-yaxunit/base_______________cf5560c21266_ed87d17ded98398ca8b3f2cec7c97f87978c4417/report.md`.

| План | Добавлено | Фактически проверено |
| --- | ---: | --- |
| F03 | 9 | Неопределённый набор хэшей, неидемпотентный консьюмер, хэш БД/порции, Null, пустой хэш порции, статус и журнал отмены. 9/9. |
| F02/F03/F04/F05/F08 | 12 | Полный путь обработки: отсутствующее правило, пустой тип/метод, успех, отказ, исключение, четыре сочетания наличия набора и идемпотентности; запись полей хэша и сохранность чужого кэша. Второе сообщение после успеха отменяется, после отказа/исключения обрабатывается. В реальном обратном вызове проверяются тело и девять свойств, распаковка заголовка. 12/12. КД и счётчик внешнего цикла ещё не покрыты. |
| D05 | 9 | Успешный ответ, общая ошибка, пустой/пробельный текст, одна/две индексные ошибки и их приоритет над общей ошибкой. 9/9. |
| D03/D04/R06 | 10 | Отсутствующий топик, ключ сообщения/объекта, partition=-1, тело/заголовок, попытка отправки, хэш отключён/совпадает/отличается/Null, поля набора хэшей. 8/10; два падения R06 после пропуска по топику либо дублю. |
| E12 | 18 | Отсутствующий/пустой/пробельный/обычный/длинный ключ; заголовки отсутствуют/пусты/несколько/дублируются; строковое/двоичное тело; partition, offset, timestamp, дробные даты загрузки; влияние регистра topic/key, консьюмера, тела и раздела на идентичность. 15/18; три падения обрезания ключа. Независимый полный вектор SHA256 XML тела ещё не добавлен. |
| E11/R02 | 5 | Существующий двойник компоненты Kafka, фактический оригинальный цикл и запись в БД внутри транзакции. Проверены лимит 7, auto.commit=true, повторно используемая сессия, пустой пакет, сохранение одного/трёх сообщений. 3/5; R02 падает после успешной записи одного и трёх сообщений. Ошибка чтения и отказ записи пока не проверены. |

Новые подтверждённые расхождения:

- **R06**: транспортный массив имеет одну запись, очередь — две. Ошибка индекса 0 относится ко второй исходной записи, но оригинал применяет индекс к полной очереди. Вторая запись остаётся «Выгружено». Оба варианта пропуска сохранены падающими.
- **E12**: сгенерированный 64-символьный SHA256 сохраняется в КлючСообщения длиной 50; явно переданный ключ длиной 51 также обрезается до 50. Ключ длиной 50 сохраняется. Два случая без key (строка и двоичное тело) и один длинный явный ключ не проходят. Сам КлючЗаписи остаётся длиной 64. Исправление метаданных продукта не выполнялось.
- **R02**: записи присутствуют в БД, но выходной флаг Загружено остаётся Ложь. Тест проверяет запись и аргументы компоненты до проверки флага, поэтому падение не скрывает результат сохранения.

Вместе с прежними R04 и E05 это девять падений по пяти причинам. Ожидания не ослаблялись, тесты не отключались.

Диагностика после revalidate_objects двух объектов: **178**, из них 158 doc-comment-type и прежние 20 других сообщений. После точки 250 добавилось 82 сообщения к именам полей документирующих комментариев в новых обёртках, конструкторах и обратном вызове; формат и фактические типы проверены по std641 и оригинальным методам. Устранены четыре новых property-return-type проверкой наличия и значения полей транспортного пакета, а также три structure-key-modification: необязательные key/headers теперь выбираются параметрами конструктора, без удаления полей возвращённой структуры. Новых замечаний других кодов после сверки по методам нет. Исторические замечания не объявляются устранёнными.

Для двойника компоненты введён контекст одного теста и очистка ПослеКаждогоТеста по lifecycle-and-contexts; общий прогон подтверждает отсутствие влияния на прежние сценарии. Реальные Kafka и Telegram в этих проверках не использовались. План остаётся в работе; отметки полных разделов и инструментальные проценты покрытия не выставлены.

### Контрольная точка: 407 сценариев

Общий прогон: **407 total, 394 passed, 13 failed, 0 errors, 0 skipped**. После точки 313 добавлены 94 сценария; с начала поручения — 306. Общий отчёт обновлён по прежнему пути. Все 13 падений относятся к шести подтверждённым причинам: прежние R02/R04/R06/E05/E12 и новый R03.

| План | Добавлено | Проверки и результат |
| --- | ---: | --- |
| C08 | 7 | Все десять свойств, CatalogObject/CatalogRef/Catalog, UUID/не-UUID, InformationRegisterRecordSet, неизвестные метаданные. 7/7. |
| C06/C09 | 13 | Обычные данные; точный двухпольный контейнер; отсутствующее поле и лишнее третье; пустые/неструктурные/непустые дополнительные свойства; перезапись совпадающего ключа; несовместимые данные; отказ, отсутствие результата, исключение, пустой тип/метод. Проверяется тело, полученное настоящим преобразователем. 13/13. |
| A07 | 26 | Все 16 сочетаний четырёх фактов, независимые опции логирования/контроля, игнорирование опций, Неопределено, пустая и неполная структура фактов. Проверяется точный полный список и наличие представлений. 26/26. |
| E07 | 6 | Карта потоков с командой/без команды и 0/1/3 назначениями; нет включения другого потока без команды; команда без назначений не формирует элемент карты. 6/6. |
| C10/F07/R03 | 42 | 16 сочетаний направления, ошибки, пропуска и исходного отказа; протокол и точные аргументы; 20 вариантов отсутствующих/пробельных обязательных полей; два направления повторного использования с однократной инициализацией; четыре независимых требования к статусу входящей обработки. 38/42; четыре падения R03. Выгрузка регистров с регистратором C12 остаётся отдельным остатком. |

**R03:** при ФлагОшибки=Ложь оригинал возвращает Ложь как результат КД и вызывающий код устанавливает ОшибкаОбработки вместо Обработано либо ОбработкаОтменена. При ФлагОшибки=Истина устанавливаются Обработано или ОбработкаОтменена вместо ОшибкаОбработки. Проверка текущего возврата флага отдельно проходит; регрессионная проверка ожидаемого смысла успеха падает для всех четырёх сочетаний. Продукт не исправлялся.

В unit через unit-edt заимствован `CommonModule.кфкОбменДаннымиXDTOСервер`; создан тестовый модуль из четырёх минимальных Around-перехватов конструктора, инициализации таблиц, чтения и выгрузки. Перехваты делегируют МокитоПерехват.АнализВызова и при отсутствии обучения вызывают ПродолжитьВызов. Бизнес-логики в них нет; base/adapter/YAxUnit не менялись. В тестах выполняется настоящий конструктор компонентов, а зависимые обмен и загрузка правил заменяются обучением. Соблюдены mocking, lifecycle-and-contexts и testability: исчерпана имеющаяся публичная граница, добавлены только швы unit.

A07 первоначально выявил ошибку фикстуры: функциональные опции не видят незавершённые изменения тестовой транзакции. Набор переведён на запись настроек вне транзакции с предварительным сохранением полного исходного набора логирования и значения константы контроля в контексте теста. ПослеКаждогоТеста восстанавливает их. Перед вызовом алгоритма проверяются фактические значения обеих функциональных опций. Повторный focused и общий прогоны проходят эти сценарии.

Последняя revalidate_objects проверила все три объекта. **253 замечания = 233 doc-comment-type + прежние 20 других сообщений**. Новые замечания типа/методов устранены: ссылка на конструктор правил полностью квалифицирована; контракт обёртки свойств допускает и выборку запроса, и используемую запись-структуру; карта описана через КлючИЗначение с массивом в поле Значение вместо неподдерживаемой записи с двумя «Из». Публичные методы обучения/проверки Мокито вызываются через конкретные модули МокитоОбучение/МокитоПроверки: EDT не разрешал цепочки через обобщённый ОбщийМодуль, хотя runtime их исполнял. Подавлений нет. Новый модуль перехватов не имеет диагностик. Дополнительные doc-comment-type относятся к корректно объявленным полям новых схем и проверены по std641 и живым конструкторам; прежний общий код не переписывался.

Остаток включает реальные/управляемые фоновые задания A/B, диспетчерские выборки и порции C/D/F, часть E02–E06/E08–E10/E13, C12, G07–G09 и прочие оставшиеся G, внешние обработчики логов H и контролируемые HTTP/Telegram границы H09/I05. Наличие тестовых перехватов КД не означает покрытия этих частей. Полный план ещё не выполнен.

## Продолжение: C12, E02, E03, E04 и E06 — 464 сценария

Добавлено 57 сценариев относительно контрольной точки 407. Все новые сценарии проходят. Полный прогон: **464 / 451 passed / 13 failed / 0 errors / 0 skipped**. Число и причины продуктовых падений не изменились.

| ID | Тест | Новых вариантов | Проверяемые результаты | Строки base |
|---|---|---:|---|---|
| C12, ветвь C10 | `ВыгрузитьЗаписиРегистра_СоблюдаетГраницыОбхода` | 12 | 0/1/3 записи, чужой регистратор, успех/пропуск/ошибка/оба флага во втором вызове; состав и порядок массива относительно вызовов КД; отсутствие повторов и вызова после ошибки; прямой обход и вызов через КД-сериализатор | 2612–2724 |
| E06 | `ОбновитьРаспределениеКонсьюмеров_УчитываетСоставПотоков` | 8 | новый/существующий/исчезнувший поток, пустой список активных; карта, добавления/удаления в БД и сохранность чужих записей | 3207–3272 |
| E03 | `РассчитатьОтставаниеПартиции_УчитываетСмещение` | 11 | none, ниже low, на low, внутри, на high, выше high; числовые и строковые смещения; точные параметры границ | 3111–3132 |
| E03 | `СтатистикаОтставанияТопика_СуммируетПартиции` | 4 | отсутствующее поле partitions, 0/1/3 партиции, суммарный размер и точные вызовы зависимости | 3087–3109 |
| E03 | `Отставание_СообщаетКонтекстОшибки` | 4 | пустой/пробельный ответ; исключение содержит топик или партицию и причину компоненты | 3087–3132 |
| E02 | `ТаблицаРаспределенияЧтенияКонсьюмеров_ПереиспользуетКоннекторы` | 13 | 0/1/3 консьюмера, 0/1/3 топика, общий/разные адреса, два прохода; минимум одна партиция, сумма очередей, история, сохранение экземпляров коннекторов | 2971–3109 |
| E04 | `ТаблицаРаспределенияЧтенияКонсьюмеров_УдаляетТолькоСтаруюИсторию` | 5 | возраст 0/299/300/301/1000 секунд, строгая граница удаления, сохранность свежего замера, сброс выходного размера очереди | 2971–3022 |

Изоляция C12: записи создаются в транзакции YAxUnit. Идентификатор записи содержит UUID регистратора без дефисов и номер строки; длина 34 укладывается в фактическое ограничение поля 36. Первые два диагностических прогона выявили коллизию и обрезание тестовых идентификаторов; подготовка исправлена, требования теста не ослаблялись. Запрос продукта не задаёт ORDER BY, поэтому тест проверяет сохранение фактической последовательности вызовов КД, а не предполагаемый порядок БД.

Для C12 существующий перехват `ПроизвестиВыгрузкуДанных` дополнен минимальной передачей вызова тестовому обработчику при наличии `ют_ВыгрузкаРегистра` в контексте. Это нужно для изменения флагов на втором вызове: МокитоОбучение.ВыполнитьМетод вызывает оригинал и не предоставляет callback. Проверяемый цикл остаётся в base. При отсутствии маркера сохраняется прежний путь Мокито/оригинала. После каждого теста маркер удаляется.

E02/E03 используют готовую подмену `ют_КоннекторИнтеграцииДляТестов` на границе внешней компоненты. Алгоритмы расчёта и объект интеграции выполняются оригинальные; реальные сетевые запросы отсутствуют. После теста контекст подмены очищается. Тест границы E04 ограниченно повторяет подготовку, если короткий вызов пересёк смену секунды, и проверяет одинаковую секунду до и после проверяемого вызова.

После изменений выполнены `revalidate_objects` для трёх затронутых модулей и focused `get_project_errors`: **267 замечаний, из них 247 doc-comment-type и 20 прежних сообщений других кодов**. Новые реальные замечания о размещении внутреннего помощника, описании структуры результата и замене типа параметров подключения исправлены. Подавлений не добавлено. Успешные прогоны не являются инструментальным замером покрытия.

Остаются прочие незакрытые сценарии плана, включая жизненный цикл ФЗ, полные циклы очередей, очистку, сетевое логирование и контроль интеграции. Цель всех тестов по плану ещё не достигнута.

## Контрольная точка: 639 сценариев

Общий прогон: **639 / 618 passed / 21 failed / 0 errors / 0 skipped**. Исходный модуль base сохраняет hash `6fdf7dc94e2cfc19`. После точки 464 добавлено 175 сценариев:

| Группа | Добавлено | Узкий результат | Проверка |
|---|---:|---|---|
| I04 | 8 | 8/8 | Независимые показатели направлений, заголовки, SLA, ошибки, порядок текста |
| G05 / H01 | 24 | 24/24 | Сроки хранения, особые значения срока хэшей, опция и адрес логирования, URI и пять флагов |
| I02 ошибки | 60 | 60/60 | Шесть статусов в обоих направлениях; нижняя исключена, верхняя включена, соседние метки |
| I02 / R05 SLA | 51 | 46/51 | Завершенные и незавершенные, длительность 4/5/6 минут при SLA 5, допустимые статусы, общие и разные диапазоны |
| I03 | 32 | 29/32 | Максимумы, Null, отсутствие одной/обеих позиций, пустые очереди, числовые счетчики, правильные UUID позиций, чужая позиция и повторный расчет |

R05 подтвержден пятью падениями: три случая просроченных незавершенных сообщений сравнивают абсолютную дату с длительностью SLA; два входящих завершенных сообщения исключаются из-за верхней границы исходящих. Исходная функция `РезультатЗапросаКонтроляИнтеграцииПоказатели`, строки 5024–5126.

Новый дефект I03 подтвержден тремя падениями: при отсутствии обеих позиций `MAX` возвращает Null для нижних границ. Первый расчет возвращает нулевые ошибки при двух реальных сообщениях в одном или обоих направлениях, но уже сохраняет максимальные позиции. Следующий запуск пропускает этот диапазон. Источник: `ВыборкаКонтроляИнтеграцииДиапазонИзменений` (4948–5022) и `КонтроляИнтеграцииПоказатели` (4904–4946). Наличие одной позиции позволяет получить ноль для другого направления; эти сценарии проходят.

Ранее подтвержденные 13 падений сохранились: балансировка E05 (1), R04 (1), R06 (2), обрезание ключа E12 (3), R02 (2), R03 (4). Всего 21 падающий сценарий по девяти причинам, включая две разные причины R05. Продуктовые алгоритмы не исправлялись.

Подготовка I03 временно очищает очереди и позиции только нужных двух UUID внутри транзакции набора YAxUnit; записи восстанавливаются откатом. Чужая позиция создается и проверяется отдельно. Для I02 расчет сравнивается до/после добавления изолированной записи. Уведомления и сетевые вызовы не выполняются.

Исправлена ошибка теста: `Дата(2025, 12, 31, 23)` не компилируется платформой, хотя EDT принимал модуль. После добавления минут и секунд все тесты снова обнаруживаются. Временный диагностический вызов из `Обр_кфкИнтеграция_МО` удален с восстановлением исходного метода; временные методы borrowed-модуля и точки останова удалены. Нулевые прогоны не учтены как успешные.

После revalidate и завершения фоновой проверки EDT: **329 замечаний: 301 doc-comment-type, 20 прежних сообщений прочих кодов и 8 новых constructor-function-return-section**. Последние относятся к четырем полям результата `ют_ВыборкаКонтроляИнтеграцииДиапазонИзменений` (строка 1210), по два сообщения на поле: анализатор видит только Null в документированном объединении `Число, Null`. Классификация — ложноположительные: исходник объявляет оба типа, а 16 выполненных сценариев подтверждают числа при наличии строк и Null при их отсутствии. Новые реальные сообщения из-за нетипизированного массива направлений устранены явным `Массив Из Булево`. Подавлений диагностики нет.

План остается незавершенным: нужны остальные циклы, ФЗ, очистка, запросы/выгрузка логов, уведомления и измерение покрытия. Эта контрольная точка не является завершением задачи.

## Продолжение: HTTP-логирование — 898 сценариев

Общий прогон `4da4e05e-a5c2-41ad-b682-9cf2cee22863`: **898 всего, 859 пройдено, 39 падений, 0 ошибок, 0 пропусков**. Хэш продукта `6fdf7dc94e2cfc19` не изменился.

| План | Добавлено | Проверенное поведение |
|---|---:|---|
| H02 | 43 | Два направления, исключение начальной позиции, лимит и порядок, старые/свежие/будущие записи; минутный запас относительно часов. |
| H09 | 48 | Реальные HTTP/HTTPS, новая и готовая сессии, два последовательных вызова, POST/path/headers/gzip/JSON, 200/201/204/400/500/разрыв. 36 пройдено, 12 падений на пустом массиве. |
| H02/R09 | 10 | Исходные запросы и обработчики с реальной HTTP-отправкой нескольких порций. Четыре записи с одинаковой меткой и одна следующая; размеры порций 1–5. Шесть падений на размерах 1–3. |
| H04/H05 | 112 | Полное сравнение полей события: заголовки, тело, исходные XML/нестроковые данные, namespace, даты, отсутствие имени настройки, удаление. |
| H01/H03/H09 | 40 | Исходный внешний цикл: ранние выходы, коэффициенты и фактические лимиты 5000/10000/20000, сохранение позиции при 500/разрыве и продвижение после успеха. |
| H03 | 6 | Пустые очереди: отсутствующая, старая и будущая позиции; отсутствие HTTP, сохранение будущей позиции. |

Локальный приёмник `scripts/http-log-test-server.py --tls` слушает только 127.0.0.1:18768/18769. Корреляция и очистка захватов выполняются по UUID каждого теста. Временный сертификат не устанавливается в системное хранилище; команды запуска и зависимости указаны в README. Gzip проверяется независимым стандартным декодером Python.

Новые воспроизведения:

- **H09, 12 падений:** gzip пустого JSON-массива не распаковывается (`Compressed file ended before the end-of-stream marker was reached`). Непустые массивы проходят. Точная причина в упаковщике gzip отдельно не доказана; продукт не исправлялся.
- **R09, 6 падений:** при четырёх сообщениях с меткой 1001 и одном с 1002 размеры порции 1/2/3 отправляют соответственно 2/3/4 сообщения вместо пяти. Следующий запрос использует строгое сравнение с меткой последнего отправленного сообщения и теряет оставшуюся часть одинаковой метки.

Изоляция функциональных опций проверена смешанным прогоном: значения меняются до транзакции очередей, собственная транзакция откатывается в hook, исходные настройки восстанавливаются после отката. Изменение опции внутри автоматически откатываемой транзакции давало устаревшее значение кэша; этот недостаток тестов устранён. Наблюдатели аргументов запросов вызывают оригинальные методы и не подменяют результаты.

Focused EDT: 375 замечаний в трёх объектах; диагностика не считается чистой. Прежние сообщения и ложноположительные схемы типов остаются; подавления не добавлялись. Точные равенства динамической верхней границе времени и пустому указателю, блокировка между порциями и полный инструментальный замер остаются открытыми.

## Продолжение: внешний цикл контроля — 100 новых сценариев

Узкий прогон тега `ЦиклКонтроля`: **100/100**, без ошибок и пропусков. I01: 81 комбинация опции, SLA, пустого/пробельного chat и token, отсутствующих/исходящих/входящих/обоих нарушений. Проверяются точный итоговый текст, единственный вызов уведомления, позиции и отсутствие повторного уведомления. I05: 19 сценариев условий уведомления об ошибке чтения и точной подстановки консьюмера/текста, включая пустой текст и нулевые SLA.

Перехват `ОповеститьОбОшибки` применяется только по маркеру контекста как граница внешнего уведомления. Проверяемые запросы, агрегаты, формирование текста и условия исполняются оригинальными методами. Этот перехват **не является проверкой Telegram HTTP**, оставшаяся часть I05 открыта. Общий модуль после добавления этих 100 сценариев ещё не прогонялся полностью.

## Продолжение: очистка, возврат и дубли — 296 новых сценариев

| План | Сценарии | Результат узкого прогона |
|---|---:|---|
| G08/G09 | 9 | УдалитьЗаписиРС: 0/1/999/1000/1001/2000/2001 хэш, порции 1000 и малые 1/2; проверка после каждой порции, сохранность двух контрольных строк на границе 1000 и выше. 9/9. |
| G07 | 39 | Устаревшие сообщения: все восемь статусов, отдельные и одновременно включенные сроки, отключение всех сроков, строгая граница 999/1000/1001, лимиты 1/3/100 и сортировка по периоду и ключу. 39/39. |
| G08 | 32 | Полные циклы исходящей/входящей очистки, блокировка до начала; 0/1/999/1000/1001/2000/2001/10001 сообщений. Свежая запись и другой статус сохранены. 32/32. |
| G09 | 12 | Служебная очистка: позиции 31/29 суток, повторяющиеся UUID отсутствующего задания, сроки хэшей -1/0/1/2, 0/1/1001 старых хэшей и свежий контрольный. Два запуска проверяют остаток порции хэшей. 12/12. |
| G03 | 96 | Отбор возврата: восемь статусов × попытки 0/2/3/4 × метки ниже/на/выше начальной; поля выбранного сообщения. 96/96. |
| G03/R10 | 12 | Полный возврат 0/1/999/1000/1001/2001 сообщений, блокировка; статус, каждая запись, сохранность тела/ключей/журнала/попыток, обновление метки и позиции, три контрольные строки. 12/12. |
| G01 | 96 | Поздняя версия того же/другого ключа × восемь статусов × занятость ключа × метки ниже/на/выше; оригинальные запрос и обработчик, отмена старой версии, журнал/дата/позиция, сохранение новой версии. 96/96. |

В G03 общий узкий прогон 108/108 занял около 136 секунд. JUnit показывает основное время в проверках каждой записи больших наборов: сценарии по 2001 сообщению занимают около 23 секунд; это не зацикливание.

Для массовых данных используются наборы записей внутри откатываемой тестовой транзакции; чтение и изменения выполнялись только через unit-edt. Непустые реальные выборки и операции записи/удаления сохраняют исходный алгоритм. Никаких изменений base/adapter для прохождения тестов не внесено.

После revalidate_objects: **416 замечаний = 388 doc-comment-type + прежние 20 + 8 подтвержденных ложноположительных сообщений Число/Null**. Новые два замечания типа элемента пустого массива в обертках выборок устранены явным описанием коллекции. Остальные прежние диагностики не подавлялись.

Остаток G: полный цикл удаления дублей и его потоковый режим/паузы, активное/неактивное настоящее ФЗ в служебной очистке, точное равенство динамической границе 30 суток, блокировка между порциями и инструментальный замер. Общий прогон всех 1294 зарегистрированных сценариев запущен; результат требует проверки.

Общий прогон `5ee95870-4f8c-4dac-9182-628a48128d13` завершен: **1294 всего, 1255 пройдено, 39 прежних падений, 0 ошибок, 0 пропусков**, 212 секунд. Все 396 новых сценариев после checkpoint 898 проходят совместно. Весь план и замер покрытия остаются незавершенными.

## Продолжение: полный цикл удаления дублей — 20 новых сценариев

Тег `ЦиклДублей`: **20/20**. Полный исходный цикл проверен с 0/1/999/1000/1001/2000/2001/10001 старыми версиями и блокировкой до начала; новые версии и занятый ключ сохраняются. Проверяются отмена, журнал, дата, метка и сохраненный указатель. Для пустой выборки проверены обычный/потоковый режим и старая/будущая исходная позиция.

В прямом серверном тесте платформа запрещает ВызватьПаузу внутри клиент-серверного вызова. В существующий перехват Пауза добавлен отдельный маркер `ют_ПаузыФоновыхОпераций`: он только записывает длительность внешнего ожидания. Оригинальный обработчик пустой выборки сохраняет позицию и возвращает свой результат. Тест подтверждает ровно одну паузу 5000 мс в потоковом режиме и отсутствие паузы в обычном. Маркер очищается после теста; прежняя ветвь `ют_ПаузыKafka` сохранена.

Диагностика измененного общего перехвата: **0 замечаний** до и после. Затронутый соседний модуль `ОМ_кфкИнтеграцияСлужебный` прогнан полностью: **81/81**, без ошибок и пропусков. В целевом модуле зарегистрировано 1314 сценариев; последний общий прогон пока относится к 1294.

## Продолжение: выборки C04/D02/F01 — 261 новый сценарий

- 141/141: статусы и граница метки, пустые/нужные/чужие настройки, занятость ключа и поздняя версия. Для исходящих проверена последняя версия; для входящих — отсутствие такого фильтра.
- 96/96: соединение назначения по UUID, ключу и дате, независимое несовпадение каждого поля, наличие/отсутствие хэша и отсутствие фильтра по статусу рабочего потока.
- 18/18: лимиты 1/2/10 и точный порядок строк всех трех направлений, отдельно диспетчер и рабочий поток.
- 6/6: группировки отправки по двум продюсерам и двум типам, одна/три строки в группе, лимиты 1/3/100, отсутствие потерь и повторов.

Все запросы исполняются оригинальными методами через минимальные экспортные обертки unit. Полный цикл диспетчера этими сценариями не покрывается.

## Продолжение: сохранение отправки D06/D07/R07 — 36 новых сценариев

32 сценария проверяют пакеты из одной/трех записей, идемпотентность, прежний хэш, успех/частичную ошибку/ошибку транспорта/некорректные счетчики. Проверены JSON пакета, заголовки, ключи, топик, таймаут 30000, статусы, счетчики, тело, удаление назначений и сохранность чужого назначения/хэша. Результат: **24 пройдено, 8 падений, 0 ошибок и пропусков**.

R07 воспроизведен: после неподтвержденной отправки сохраняется SHA256 нового тела, включая замену прежнего хэша. Четыре отдельных сценария выполняют ошибочную отправку, заново назначают запись потоку и вызывают оригинальную подготовку следующего пакета по свежей выборке. Во всех четырех случаях ожидаемая одна запись отсутствует в пакете: **0 пройдено, 4 падения**, без ошибок и пропусков. Это подтверждает пропуск повторной отправки, а не только преждевременную запись хэша.

Некорректные счетчики проверены как исключение до записи результата; отказ записи регистра остается отдельным незавершенным пунктом D07. Продукт не изменялся. Всего зарегистрировано **1611** сценариев; полный прогон `bbfdcbb5-9390-46c1-b7b3-83ec36300b9c` запущен, результат пока не подтвержден. После повторной валидации четырех объектов EDT: **476 замечаний**; чистая диагностика не заявляется.

Общий прогон `bbfdcbb5-9390-46c1-b7b3-83ec36300b9c` завершен за 246 секунд: **1611 всего, 1560 пройдено, 51 падение, 0 ошибок, 0 пропусков**. Это 39 прежних падений и 12 новых регрессий R07. Остальные добавленные сценарии проходят совместно.

## Продолжение: одиночная обработка и запись пакета — 48 сценариев

C03/F01: **24/24** — прямые публичные СериализацияСообщения/ДесериализацияСообщения. Проверены совпадение/несовпадение ключа и даты, занятость того же или другого периода ключа, обработка при отсутствующем правиле, счетчик, журнал, метка, сохранность другой версии, чужого сообщения и назначения.

C05/F04/F08: **24/24** — оригинальные обработчики пакета с одной/тремя записями, успехом, отказом, исключением произвольного обработчика, неопределенным результатом, пустым типом и отсутствующим правилом. Проверены все записанные статусы, счетчик ровно +1, метки/длительность, дополнение журнала, тело и ключ, вызовы обработчика по порядку, удаление только своих назначений и сохранность контрольной записи. Произвольный callback — внешняя граница; код обработки и записи результата не подменяется. КД и идемпотентность полного входящего пакета остаются отдельными пунктами.

Всего зарегистрировано **1659 сценариев**. Последний общий прогон относится к 1611; последние 48 проверены узкими прогонами. Следующий этап — реальные фоновые задания A/B. План и инструментальный замер остаются открытыми.

## Продолжение: настоящие фоновые задания A08/A10/A13/B01/B02 — 10 сценариев

Запуск — **4/4**: прямой вызов и вложенные номера 01/12/99. Оригинальные ЗапуститьФЗ/ЗапуститьПоток проверяются с массивом аргументов и Неопределено, подтверждением готовности отдельного сеанса, точными ключом/методом/наименованием, переиспользованием активного задания без дубля и новым UUID после завершения. ТекущийПоток вызывается оригинальным методом внутри настоящего ФЗ; его UUID возвращается родителю сообщением готовности.

Управление состояниями — **6/6**: оригинальные АктуализироватьПотоки и ЗавершитьПотоки с отменой/без нее, пустым/непустым списком. Подготовка подтверждает активное, завершенное, аварийное и отмененное состояние; добавлен отсутствующий UUID. Проверены состав оставшихся назначений, состояния заданий и сохранность контрольного активного задания вне переданного списка. Узкий прогон `f0c0cbf9-5437-4d17-bebb-8908b2bc5a01` завершен за 65 секунд без ошибок и пропусков.

Минимальный обработчик не исполняет алгоритмы продукта: сообщает UUID/аргумент, ждет свой временный файл не дольше 30 секунд и по параметру завершает работу ошибкой. Очистка отменяет только UUID созданных тестом заданий и удаляет только их временные файлы. Изменения очереди выполняются в отдельной откатываемой транзакции после подготовки состояний; дочерние сеансы ее не используют.

B03: **5/5** — оригинальное ожидание для пустого списка, свободного активного задания, завершенного задания с оставшимся назначением, завершения во время ожидания и освобождения назначения активного задания. Последние два случая проходят ровно одну паузу 5000 мс, остальные — ни одной. Контрольное назначение сохраняется.

Для детерминированного изменения внешнего состояния перехват тестовой паузы выполняет один callback из контекста и предварительно удаляет его маркер. Callback либо подает сигнал настоящему ФЗ и дожидается завершения, либо удаляет тестовое назначение, моделируя освобождение очереди работником. Оригинальные цикл, запрос, проверка состояния и очистка завершенных потоков не подменяются. Маркеры удаляются после теста. Диагностика общего перехвата — 0; соседний модуль ОМ_кфкИнтеграцияСлужебный повторно прошел **81/81**.

Всего **1674 сценария**. Последний полный результат: 1611/1560/51, последние 63 сценария прошли узкие проверки. Полный повторный прогон 1674 требуется после последних изменений описаний типов.

Общий прогон `ad329f09-d7a0-4e9c-93cd-89a57b4135a9`: **1674 всего, 1623 пройдено, 51 прежнее падение, 0 ошибок и пропусков**, 337 секунд. Новые одиночные/пакетные сценарии и настоящие ФЗ проходят совместно. Диагностика перед прогоном: 515 замечаний, из них 466 doc-comment-type. Остальные включают прежние сообщения, замечания документации/типизации новых оберток и два предупреждения о временных файлах; удаление файлов выполняется отдельным after-hook, без подавления диагностики.

## Продолжение: свободные потоки, служебная очистка и раздел — 37 сценариев

- B04: **12/12** — первый поток, свободный существующий, создание второго до лимита, ожидание занятого потока на лимите; Неопределено/массив/скаляр параметров. Проверены переданные аргументы в отдельном сеансе, UUID, ключи, состав списков, одна пауза при занятости и сохранность контрольного назначения.
- B04: **8/8** — перебор номеров: лимиты -1/0/1/2/3, отсутствие потока, все номера заняты, свободный номер между 1 и 3, свободный первый при занятом втором. Ветка исключения вызывающего ОжидатьСвободныеПотоки при невозможности получить UUID отдельно не воспроизведена.
- G09: **2/2** — служебная очистка настоящих активного/завершенного/аварийного/отмененного заданий и отсутствующего UUID, по одному/три назначения на UUID, повторный вызов. Сохраняются только назначения обоих активных заданий.
- B05: **15/15** — пустой/один/несколько элементов, шесть перестановок трех настроек и повторяющиеся ссылки. SHA256 эталонов фиксированных UUID независимо рассчитан Python hashlib; сами тестовые объекты создаются публичным УстановитьСсылкуНового YAxUnit в транзакции. Перестановка сохраняет хэш; состав и кратность влияют на него. Представления разделены вертикальной чертой, их порядок не предполагается. При повторе ссылки представление остается одним, а хэш учитывает повтор.

Всего зарегистрировано **1711 сценариев**. Последний общий прогон относится к 1674; последние 37 проходят узкими прогонами. Остальные пункты плана и инструментальный замер не объявляются завершенными.

## Продолжение: пустая и непустая выборка распределения — 44 сценария

B10: **16/16** — интерактивность × потоковая опция × наличие указателей × исходная активность. Проверены флаг продолжения, единственная пауза 5000 мс, статусы -1/0/1, сохранность меток и чужого назначения. Созданы собственные активные ФЗ; опции восстановлены после отката тестовых данных.

B09: **28/28**, job `913cf03c-5f7f-4f87-9a94-2f6671bad10c`, 161 с. Исходящие/входящие, 1/9/10/11/999/1000/1001 сообщений, максимум пакета 1000/10 и минимум 10, два настоящих активных ФЗ. Оригинальные запрос и распределитель назначают каждый ключ ровно один раз, выбирают дату соответствующего направления, сохраняют указатели полных порций/хвоста, очищают набор после записи. Чужое назначение и исходные статусы/метки сохранены. Освобождение назначений моделируется callback внешнего работника на границе паузы; сам алгоритм распределения не подменен. Этот тест не объявляется проверкой обработки сообщений дочерними сеансами.

Всего зарегистрировано **1755 сценариев**; последний общий прогон — 1674/1623/51, последние 81 проходят узкими прогонами. Полный план и инструментальное покрытие пока не завершены.

## Продолжение: повторная десериализация F03/F05 — 40 сценариев

Тег `ПовторнаяДесериализация`: **40 всего, 38 пройдено, 2 падения, 0 ошибок и пропусков**. Два сообщения одного ключа обрабатываются оригинальными выборкой и пакетным обработчиком, в одной или двух порциях. Проверены одинаковое/измененное тело, отсутствие/совпадение/отличие старого хэша, отсутствие набора хэшей, неидемпотентный консьюмер, успех/отказ/исключение первого вызова. Повтор после отказа и исключения проходит; промежуточное состояние хэша между порциями и контрольные данные сохраняются. Эталоны SHA256 abc/abd рассчитаны независимо; при подготовке базы используется верхний регистр штатной hex-строки платформы.

Новый дефект F03/F05: два успешных сообщения одного ключа с разными телами в одной порции добавляют две строки в набор хэшей. Оригинальная запись Слияние в строке 3601 падает с конфликтом ключа регистра. Воспроизведено при отсутствующем и отличающемся прежнем хэше; разделение на две порции проходит. Продукт не исправлялся. Тест ожидает успешную запись и остается красным, не утверждает дефект как норму.

Для намеренно воспроизводимой ошибки записи используется собственная транзакция: после исключения она откатывается до проверки утверждения. Это устраняет вторичные ошибки after-hook YAxUnit после аварии транзакции; успешные сценарии откатываются штатным тестовым after-hook. Всего зарегистрировано **1795 сценариев**. Последний общий результат остается 1674/1623/51; новые узкие прогоны добавили еще два падения одной причины.

## Продолжение: отбор диспетчеров A04 и отсутствие настроек A05 — 224 сценария

A04, тег `ТаблицаДиспетчеров`: **128/128**. Оригинальный запрос проверен для продюсеров/консьюмеров, общего/собственного диспетчера, 0/1/3 настроек, всех сочетаний трех блокировок (обработка, транспорт настройки, транспорт брокера). Проверены имя и лимиты диспетчера, исключение помеченных настройки/брокера, групп и ссылок на отсутствующего брокера, сохранение контрольного элемента. Группы создаются публичным ЮТест.Данные().СоздатьГруппу; реквизит Брокер недоступен для группы и ей не присваивается. Следовательно, отсутствие групп в результате не объявляется независимым покрытием условия ЭтоГруппа от внутреннего соединения с брокером. Реальный запуск полного диспетчера остается отдельным пунктом.

A05, тег `ДиспетчерБезНастроек`: **96/96**. Два направления × пустая таблица/все настройки заблокированы/только чужой диспетчер × 16 исходных комбинаций четырех флагов. Оригинальный ОбработатьДиспетчерЗадач сохраняет все флаги и не меняет таблицу. Ветка действительного запуска с доступными настройками не подменяется этими проверками.

Всего **2019 сценариев**. Устранены короткие имена До и смена типа набора/номера в новых одиночных и пакетных тестах, уточнены типы оберток запуска ФЗ. Общий прогон после этих изменений требуется; последние подтвержденные итоги — полный 1674/1623/51 и узкие новые группы, включая два новых падения записи хэшей F03/F05.

Общий прогон `98d5e8ee-cd87-4301-b7dc-2619f3c261ef`: **2019 всего, 1966 пройдено, 53 падения, 0 ошибок и пропусков**, 639 с. Два новых падения — конфликт хэшей F03/F05; остальные прежние. Проверены совместная изоляция новых групп и устранение вторичных ошибок after-hook. Диагностика четырех объектов перед запуском: **559 = 522 doc-comment-type + 37 прочих**. Повторная инвентаризация unit-edt/base подтверждает 65 процедур и 97 функций, 5262 строки; инструментальным покрытием эта инвентаризация не является.

## Продолжение: интерактивный запуск, потоки загрузки и ошибки подключения — 37 сценариев

- A09 `ИнтерактивныйЗапуск`: **8/8**. Продюсер/консьюмер × обработка/транспорт × результат структуры/Неопределено. Проверены все девять аргументов внешней ДлительныеОперации.ВыполнитьПроцедуру, включая UUID формы, имя/ключ, единственную настройку, лимит 1, интерактивность и флаги фонового запуска/расширений. В существующий заимствованный unit-модуль ДлительныеОперации добавлен минимальный Around для публичного МокитоПерехват.АнализВызова; без настройки он вызывает оригинал. Исходная сборка параметров кфкФоновыеОперацииСлужебный не подменена. Соседний ОМ_кфкИнтеграцияСлужебныйВызовСервера прошел **13/13**. Новая обертка сохраняет обязательную сигнатуру зависимости из девяти аргументов; предупреждения о числе/порядке параметров не подавлялись.
- E08 `НедостающиеПотокиЗагрузки`: **11/11**, job `7e1ef94e-96ca-4ccd-89ce-8c82f5beb0bb`, 74 с. Ноль/один/три требуемых потока, достаточное и избыточное количество, свободные номера до/между/после существующих. Настоящие задания, оригинальный запуск и оригинальная запись команд. Проверены порядок UUID, метки новых команд, сохранение старых команд/чужого потока, сброс старого отбора и буфера только при добавлении. Все собственные задания учтены для очистки, в том числе при частичной ошибке.
- E11 `ОшибкаЧтенияПакета`: **18/18**. Отказ инициализации/подписки/таймаута × 0/1/3 прежних сессии × опция уведомлений. Проверены точная кратность остановок, отсутствие чтения и перехода к следующим консьюмерам, исключение с текстом компоненты, Загружено=Ложь, сохранность входящей очереди и уведомление на существующей изолированной границе.

Зарегистрировано **2056 сценариев**; последний полный прогон — 2019/1966/53. Новые 37 проходят узкими прогонами. Полный план и замер покрытия не завершены.

E13 `ОтказЗаписиВходящих`: **4/4** — 1/3 сообщения, несовместимый отбор по ключу/дате. Оригинальное чтение получает пакет с enable.auto.commit=true, а оригинальная запись набора отклоняет строки, не соответствующие отбору. Исключение не скрыто, Загружено остается Ложь, транзакция теста откатывается, сообщения не сохраняются. Для инъекции использован действительный отказ записи платформы, без замены пакетного обработчика. Это подтверждает риск раздельного чтения и записи при автофиксации, но не доказывает фактическую фиксацию offset или потерю на настоящем Kafka: брокер заменен внешней компонентой, время автокоммита в нем не измерялось. Нельзя объявлять гарантию доставки доказанной этим тестом. Зарегистрировано **2060 сценариев**.

## C02/F01: рабочие циклы обработки — 11.09.2026

Добавлены 24 сценария `РабочийЦиклОбработки_ОбрабатываетНазначенияИПереходитКОжиданию`: сериализация/десериализация, 0/1/3/1001 сообщений, блокировка до выборки, немедленные назначения и поступление назначений на первой паузе. Узкий прогон — 24/24, без ошибок и пропусков. Проверены исходные запросы и пакетные обработчики, порядок и однократность обработки, статусы, счетчики, журнал, тело и метки, удаление собственных назначений и сохранность чужого.

Текущий сеанс подменяется только при явном маркере контекста теста. В остальных сеансах обертка вызывает оригинал, в том числе при отсутствии контекста YAxUnit. Подмена находится в служебной области; продуктовый алгоритм не изменен.

Ограничение: изменение константы блокировки внутри транзакции не меняет возвращаемую функциональную опцию. Поэтому активный цикл завершается контролируемым исключением внешней паузы `unit-worker-idle-stop`, которое тест проверяет отдельно. Это подтверждает обработку и переход к ожиданию, но не штатный выход по поздней блокировке или границе 300 секунд; эти пункты остаются открыты. Начальная блокировка проверена с реальной функциональной опцией и штатным возвратом. Данные откатываются, исходные опции восстанавливаются обработчиком После.

Зарегистрировано 2084 сценария. Последний завершенный общий прогон пока относится к 2019 сценариям; новый контрольный прогон выполняется отдельно.

## C05/F04: пакетная обработка КД — 11.09.2026

Добавлены 16 сценариев `КДПакет_СохраняетСтатусыЖурналИЧужиеНазначения`: оба направления, 1/3 сообщения, четыре сочетания ФлагОшибки/ФлагПропустить. Оригинальные пакетные обработчики записывают реальные регистры; подменяется только внешняя граница конвертации данных существующим Мокито-перехватом. До проверки статусов проверяются вызовы КД, счетчики, журнал, тело, временные метки и сохранность чужого назначения.

Результат: исходящие 8/8; входящие 0/8 из-за ранее подтвержденного R03 — инвертированного смысла ФлагОшибки. Новые восемь падений относятся к тому же дефекту, а не к восьми новым дефектам. Ожидаемые статусы не подстроены под ошибочное поведение продукта.

## E10: интерактивный рабочий цикл чтения — 11.09.2026

Добавлены 9 сценариев `РабочийЦиклЧтения_ЗавершаетсяПоКомандеИЗакрываетСессии`: 0/1/3 консьюмера и отсутствие своей команды/удаление после первой/после второй итерации. Все 9 проходят. Проверены отсутствие чтения без команды, число чтений, однократная инициализация сессий при двух итерациях, закрытие всех созданных сессий при штатном выходе, сохранность чужой команды и отсутствие стартового ожидания 60 секунд в интерактивном режиме. Обычный режим с реальной минутой ожидания и границы простоя 300 секунд остаются открыты.

Повторный тест реальных ФЗ после добавления подмены текущего сеанса — 4/4. Первоначальный запуск с некорректным фильтром (имя метода без модуля), job ef0021c9-bc21-4c2d-8b22-cee86e72522c, остановлен после отсутствия отчета; он не является общим прогоном и не засчитывается. Инструмент сообщил, что остановка клиента не откатывает базу. Корректный повторный запуск использовал формат Модуль.Метод и завершился успешно.

Зарегистрировано 2109 сценариев. Совместный узкий прогон КД и чтения: 25 всего, 17 пройдено, 8 падений, 0 ошибок и пропусков. Последний завершенный общий прогон остается 2019/1966/53.

## C01/D01/F01: пустые диспетчеры — 11.09.2026

Добавлены 72 сценария `ПустойДиспетчер_УчитываетРежимИСохраняетСвоюПозицию`: три исходных диспетчера, 0/1/3 настройки, независимые сочетания потокового режима, блокировки и интерактивности. Все 72 проходят. SHA256 разделов для фиксированных UUID рассчитаны независимо Python hashlib. Проверены идентификаторы операций, сохранение позиции пустой выборки с отступом 900000 мс, число пауз и сохранность другой операции и чужого назначения.

Непрерывный потоковый режим без блокировки ограничивается исключением второй внешней паузы после завершения первой итерации и записи позиции; остальные варианты возвращаются штатно. Это не проверка позднего изменения функциональной опции. В расширении зарегистрирован 2181 сценарий; начат общий прогон job 1d3b5621-5ec0-430f-b9c2-09eeb2579bcb, результат пока не получен.

## Общий прогон 2181 и продолжение — 11.09.2026

Прогон `1d3b5621-5ec0-430f-b9c2-09eeb2579bcb` завершен: **2181 всего, 2120 пройдено, 61 падение, 0 ошибок и пропусков**, 785 с. Восемь дополнительных падений относительно прогона 2019 относятся к пакетной КД-десериализации и прежнему R03. Продукт не изменялся.

### C01/D01/F01: непустые диспетчеры

Добавлены 30 сценариев `ДиспетчерОчереди_РаспределяетВсеСообщенияИСохраняетПозицию`: три направления, интерактивность, 1/1001 сообщений с начальной блокировкой и без нее, 2001 сообщение без блокировки. Узкий прогон `cdf9b432-e01c-4df5-8547-f59a78b7857d`: **30/30**, 189 с.

Оригинальные диспетчеры выполняют запросы, распределение и запись позиций. Проверяются все ключи без повторов, принадлежность назначений собственным ФЗ, размеры запросов 2000/20000, отсутствие потери хвоста, позиции с отступом 900000 мс и сохранность чужого назначения. Наблюдатели запросов всегда вызывают оригинал и только записывают аргументы и число строк при явном маркере.

Завершение работы внешнего исполнителя имитируется на границе паузы: фикстура принимает назначения и меняет статус исходных сообщений. Поэтому это проверка диспетчера, а не сквозная проверка диспетчера вместе с реальным обработчиком сообщения в дочернем сеансе. Позднее переключение функциональной опции остается отдельным незавершенным пунктом.

### A01: регламентный запуск

Добавлены 8 сценариев `РегламентныйЗапуск_ПроверяетНачалоИФункциональныеОпции`: интеграция включена/выключена, блокировка включена/выключена, ошибка начала задания/успех. Узкий прогон: **8/8**. Проверены порядок начала и запуска потоков, точный объект метаданных, исключение и независимое действие двух реальных функциональных опций. Опции устанавливаются до транзакции данных и восстанавливаются после отката.

В unit добавлены два контекстных Around: внешнее начало задания БСП и запуск потоков для проверки управляющего метода. Без маркера оба вызывают оригинал. Тест не запускает реальные интеграционные операции и не отключает существующие регламентные задания.

Диагностика пяти прежних объектов — 578 замечаний; после включения ОбщегоНазначения в область проверки — 583. Среди дополнительных пяти: ранее существовавшая typed-value-adding-to-untyped-collection на строке 26 и четыре extension-method-visible-mode на строках 16, 54, 75, 99. Последние классифицированы как ложные по совпадающим контекстам модулей base/unit, корректной связи заимствования и экспортным исходным методам. Предупреждения не подавлялись. Остальные диагностические замечания остаются открытыми; чистая диагностика не заявляется.

Зарегистрировано **2219 сценариев**. Общий отладочный прогон с профилированием начат; до получения отчета результат не засчитывается. API профайлера ограничивает подробную выдачу 200 строками на модуль, поэтому такая выдача сама по себе не доказывает процент покрытия всех исполняемых строк и ветвей.

## Общий отладочный прогон 2219 — 11.09.2026

JUnit: `C:\EDT\tmp\edt-mcp-yaxunit-debug\base-1789083294252-4\junit.xml`. Результат: **2219 всего, 2158 пройдено, 61 падение, 0 ошибок и пропусков**, сумма времени наборов 1148,444 с. Состав 14 групп падений совпадает с прогоном 2181; новые диспетчеры и A01 проходят совместно с остальными тестами.

Замер начат на регистрации и остановлен на последнем параметрическом сценарии A01 после вызова проверяемого метода. Идентификатор свежего измерения: `22888774-13df-46b0-93d7-e57ad3548ea4`. Сразу после остановки API еще возвращал прежний сеанс; после завершения публикации получены данные нового измерения. Контрольная точка удалена, клиент продолжен и штатно записал JUnit.

Исходный модуль `base.Общие модули.кфкФоновыеОперацииСлужебный.Модуль`: API вернул ровно 200 строк с номерами 17–639, относящихся к 9 методам. Это первые строки ограниченной выдачи, а не полный список исполненных строк или методов. Две записи модулей unit тоже ограничены 200 строками. По этим данным нельзя объявить остальные методы неисполненными, вычислить точный процент покрытия или подтвердить обе стороны каждой ветви. Исходный хэш base на момент прогона — `6fdf7dc94e2cfc19`, основного тестового модуля — `ab3d5964cccd63e9`.

## A03: отдельный стенд

Создана собственная пустая файловая база `C:\EDT\tmp\kfk-background-operations-isolated-20260911-a03`, приложение EDT `01e91541-f1b7-4e28-81c7-6cd8c16b1bc8`, запуск `unit Изолированный стенд A03`. Конфигурация успешно загружена через unit-edt; настройка приложения по умолчанию не запрашивалась. Основной запуск всегда адресуется явным именем `base Тонкий клиент`.

Новый модуль `ОМ_кфкФоновыеОперацииСлужебныйИзолированныйСтенд` содержит два сценария создания и повторного получения регламентного задания. Перед обращением к заданиям проверяется точная строка соединения отдельного стенда; на другой базе тест отмечается пропущенным и не засчитывается в покрытие. Очистка адресует только UUID, созданный самим тестом. После добавления комментариев к экспортным обработчикам диагностика нового объекта — 0 замечаний. Узкий прогон начат, результат пока не получен.

## A02 и остановка проверки окружения — 11.09.2026

В основной модуль добавлены 6 сценариев `ПолучитьПотоки_ОтбираетОперациюИСостояние`: 0/1/4 собственных заданий, обычное имя сериализации и имя с суффиксом ВПотоке, отсутствие фильтра состояния и четыре состояния. Постороннее задание проверяется на исключение из результата. Подмена касается только содержимого собственных дочерних заданий с явным параметром-маркером; оригинальный ПолучитьПотоки и платформенный отбор не подменяются. Без маркера обе обертки вызывают оригинальные методы. История существующих заданий не очищается, поэтому вариант с нулем собственных заданий не доказывает глобально пустую историю платформы.

Всего зарегистрировано 2227 сценариев: 2225 в основном модуле и 2 в отдельном модуле A03. Последний подтвержденный общий результат остается 2219/2158/61/0/0. Новые 8 сценариев не засчитаны как пройденные.

Последняя завершенная диагностика шести основных объектов после регистрации A02: 596 замечаний. Новые замечания к типам структуры, типам элементов массива и числовому описанию параметра устранены. Дополнительное missing-temporary-file-deletion на строке 5198 A02 классифицируется как ложное: путь добавляется в ют_СигналыФоновыхЗаданий до запуска, а существующий обработчик После удаляет каждый собственный путь после завершения собственных ФЗ. Еще 12 замечаний относятся к неизмененной фикстуре регистра examples (теперь строки 11186–11194): variable-value-type, dynamic-access-method-not-found, bsl-legacy-check-static-feature-access-for-unknown-left-part и property-return-type. Объект регистра в examples существует; причина неразрешения этой ссылки в unit окончательно не установлена. Эти 12 остаются неопределенными, не подавлены; чистая диагностика и завершенность проверки не заявляются. Новый отдельный модуль A03 ранее прошел собственную диагностику с нулем замечаний.

Первый прогон A03, job `f63d395c-2d48-4fd3-a220-cafd9591770f`, остановлен после 498 с без пригодного JUnit. Журнал собственной базы показал ConfigExtensionApplyError: расширения МодульныеТесты и ТестированиеАдаптераKafka подключены в безопасном режиме, который запрещает необходимые расширения модулей. Остановка клиента не откатывает базу. Отдельная база и конфигурация запуска сохранены; результат A03 не засчитывается.

Затем `unit-edt.run_yaxunit_tests` для существующего запуска `base Тонкий клиент`, тега ОтборРеальныхПотоков, вернул обязательную ошибку подготовки: `Pre-launch preparation failed: Application not found:` (идентификатор в сообщении пуст). Это отказ подготовки, не падение теста. По Evidence and Failure Gates из общего AGENTS.md зависимая инженерная работа остановлена; повторный запуск с обходом подготовки, другим MCP или прямым доступом к исходникам не выполнялся. Для продолжения нужно восстановить однозначную привязку основного запуска к приложению `c408ec97-28c7-46ec-8598-02aff4b31e67`, подготовить режим расширений отдельного стенда и начать новый пользовательский запуск работы. Причина потери разрешения приложения пока не установлена.

## Повторная проверка — 11.09.2026, 04:18 МСК

После нового запроса пользователя все четыре проекта unit-edt подтверждены ready. У старого запуска base Тонкий клиент обнаружен синтетический applicationId `launch:base Тонкий клиент`, а у основного приложения сохранен действительный UUID `c408ec97-28c7-46ec-8598-02aff4b31e67`. Создан запуск `unit Основная база тестов` с явной привязкой к этому UUID; привязка проверена повторным list_configurations. Старый запуск не удален.

Через новый запуск выполнен тег ОтборРеальныхПотоков: **A02 — 6/6, 0 падений, ошибок и пропусков**. Отчет: `C:\EDT\tmp\edt-mcp-yaxunit\unit______________________6cf290a8f659_ff3f3c06c035f1746d84f3415df23f126a1c8647\report.md`. Ошибка Application not found для этого пути запуска устранена. Общий прогон всех 2225 основных сценариев не повторялся; последний общий отчет остается 2219/2158/61/0/0. Диагностика шести объектов повторно вернула 596 замечаний.

Повторный запуск A03 `e0d154d6-f306-4c1a-a170-371ccea2b2d8` вновь зарегистрировал ConfigExtensionApplyError в 04:17:13–04:17:15 МСК: расширения МодульныеТесты и ТестированиеАдаптераKafka подключены в безопасном режиме. Собственный запуск остановлен без пригодного JUnit и без отката базы. Два сценария A03 остаются непроверенными; режим расширений отдельной базы не менялся. Всего зарегистрировано 2227 сценариев; последний узкий успешный прогон — A02 6/6. Исходники тестов и продукта в этой повторной проверке не менялись.

## A03 после исправления режима расширений — 11.09.2026

После изменения пользователем режима расширений отдельного стенда оба сценария начали выполняться. Первый повтор выявил ошибку самой фикстуры: вызов УстановитьПривилегированныйРежим(Ложь) из обработчика ПослеКаждогоТеста, который этот режим не устанавливал, приводил к двум ошибкам очистки. Удален этот вызов; сохранена проверка равенства текущего режима исходному. Очистка по UUID собственного задания и проверка отсутствия оставшихся заданий сохранены. Продукт не менялся.

Повторный узкий прогон: **A03 — 2/2, 0 падений, ошибок и пропусков**. Подтверждены создание ровно одного выключенного задания на пустом стенде, сохранение свойств и UUID при повторном получении, отсутствие дубликатов, удаление собственного задания и восстановленное состояние режима. Диагностика CommonModule.ОМ_кфкФоновыеОперацииСлужебныйИзолированныйСтенд до и после правки — **0 замечаний**.

Отчет: `C:\EDT\tmp\edt-mcp-yaxunit\unit_____________________A03_c15aeeb558_a50d6eda4d8b9566b59191f1191675a331e30aa5\report.md`. Обе прежние проблемы запуска A02/A03 сняты. Последний общий прогон остается 2219/2158/61/0/0; дополнительные A02 6/6 и A03 2/2 проверены отдельно. Полный план и полный инструментальный процент покрытия пока не завершены.

## Продолжение A04 и отказ обязательной проверки — 11.09.2026

Исходная готовность подтверждена: unit-edt работает на 8768, все четыре проекта
ready; хэш исходного модуля base — `6fdf7dc94e2cfc19`. Продукт не менялся.
Все чтения и изменения src в этом продолжении выполнены через unit-edt.

В основной тестовый модуль добавлены:

- `ОбработатьДиспетчерЗадач_ПередаетНастройкиИЛимиты`: 48 регистраций,
  два направления, общий/собственный диспетчер, 0/1/3 настройки и все сочетания
  независимых блокировок обработки/транспорта. Проверяются точные ключи, методы,
  представления, снимки четырех параметров запуска, лимиты 2/3, отсутствие
  запуска по чужой строке и соответствующие флаги операций.
- `НовыйЖурналЗапускаДиспетчера`: типизированная таблица снимков запросов запуска.
- `ЗаблокироватьИнтеграциюНаПаузе`: подготовлен обработчик изменения реальной
  функциональной опции между итерациями. Сценарии с ним еще не добавлены.

В заимствованный модуль unit.кфкФоновыеОперацииСлужебный добавлен Around
`ют_ЗапускФЗДляПроверкиДиспетчера`. Только при маркере
`ют_ЖурналЗапускаДиспетчера` он сохраняет запрос запуска без дочернего сеанса;
без маркера делегирует оригинальному ЗапуститьФЗ. Проверяемый
ОбработатьДиспетчерЗадач выполняется оригинальным кодом. Этот перехват не
является покрытием ЗапуститьФЗ и не закрывает A10.

Расчетное число регистраций — 2275 (2273 основных и 2 A03); runtime discovery
не повторялся. Новые 48 сценариев не запускались: пользователь попросил сначала
добавить весь остаток. Последний общий и узкие результаты не изменились.

До изменений диагностика основного тестового модуля дала 193 замечания,
заимствованного — 389. После адресной ревалидации и регистрации A04 — 197 и 397.
Четыре doc-comment-type в новом конструкторе журнала называют типами имена
колонок Ключ, ИмяМетода, Наименование и Параметры, хотя их тип Строка явно указан
и колонки создаются с ОписаниеТипов("Строка"). Это ложные срабатывания по
сопоставлению исходника с std453, std641 и v8cs:doc-comment-type из v8std;
подавлений не добавлено. В новом тесте, его регистрации и Around замечаний нет.
После ревалидации также появились/перегруппировались constructor-function-return-section
в прежних обертках инициализации кэша (строка 501) и диапазона контроля (1592).
Эти участки не менялись; у первого описаны возвращаемые поля, у второго
Число/Null, но причины различий диагностики окончательно не установлены.
Чистая диагностика и полный сопоставимый итог шести объектов не заявляются.

Последняя мутация: добавление ЗаблокироватьИнтеграциюНаПаузе, основной модуль
вырос с 12167 до 12175 строк; синтаксическая проверка записи прошла.
Следующий обязательный revalidate_objects для
CommonModule.ОМ_кфкФоновыеОперацииСлужебный вернул success=false:
`Project is building: com._1c.g5.v8.derived.DerivedDataStatus@1fadcaf7. Please wait and retry.`
Дальнейшая инженерная работа остановлена по Evidence and Failure Gates общего
AGENTS.md; проверка последней правки не завершена. Встроенное описание
doc-comment-type также недоступно в EDT; нормативная карточка v8std была доступна.

После нового пользовательского запуска сначала подтвердить готовность unit-edt,
завершить адресную проверку последнего изменения и проверить актуальные номера
строк. Затем продолжить добавление оставшихся тестов, начиная с поздних
блокировок и циклов C/D/F/E09/E10. A04 также еще требует проверки оркестратора
ЗапуститьПотоки целиком и смешанных строк блокировок. Остаток A02/A10, D04/D07,
E11–E13, G06/G08/G09, H02/H03, I05 не объявлен закрытым. После всех групп —
сверка матрицы, узкие проверки, общий прогон и инструментальное покрытие.

## Продолжение после повторного запуска — 11.09.2026 (работа продолжается)

Готовность unit-edt:8768 восстановлена. Проверка последнего обработчика поздней
блокировки завершена. Продукт не менялся; операции с src выполняются через EDT.
Пользователь просит сначала добавить весь остаток, поэтому новые тесты пока
не запускались. Предыдущие результаты запусков остаются исторической точкой.

Добавлены и зарегистрированы: E09 — 18 сценариев завершения диспетчера чтения
и нулевого лимита; C/F — 12 сценариев поздней блокировки обработки; D — 24
сценария рабочего цикла отправки с порциями до 2001 записи; E10 и C/D/F — 6
сценариев реальной стартовой минуты и 300-секундного простоя; G06 — 16 сценариев
совместных сроков и группировки статусов; D07 — 8 сценариев исключения внешнего
транспорта и повторной отправки; D04 — 12 сценариев двух сообщений с одинаковыми
и разными телами. Вместе с предыдущими 48 A04 расчетное число регистраций —
2371 (2369 основных + 2 A03). Runtime discovery еще не повторялся.

Новые тесты вызывают оригинальные обработчики. Двойник внешней Kafka-компоненты
поддерживает настройку исключения через ключ ют_Ошибка:<имя метода>; поведение
без такого ключа сохранено. Четыре сценария простоя используют реальное время
без подмены внутреннего таймаута и увеличивают длительность будущего прогона
примерно на 20 минут; тест стартовой минуты — еще примерно на минуту.

Основной модуль до добавления пары сообщений сохранял 197 замечаний. После D04
появились два property-return-type на извлечении message/key; исправление этих
новых замечаний выполняется. После изменения внешнего двойника EDT показал 31
замечание вместо первоначального 0: module-accessibility-at-client для прежних
методов и двух переменных, invocation-parameter-type-intersect в прежнем методе
записи текста (строка 361). Эти определения не изменялись; полностью сопоставимая
чистая диагностика не заявляется. Подавления не добавлялись.

Все эти группы требуют runtime-проверки и сверки матрицы. Добавление остальных
сценариев продолжается; этот раздел не означает завершения плана.

## Контрольная точка перед повторным запуском — 11.09.2026

После предыдущей промежуточной записи дополнительно зарегистрированы E13 — 2
сценария ЗагрузкаПакета_ПослеОтказаЗаписиНеСохраняетПрочитанныеСообщения и E11 — 9
сценариев ЗагрузкаПакета_СохраняетСообщенияВсехКонсьюмеров (0/1/3 консьюмера и
0/1/3 сообщения). E13 использует несовместимый отбор реального набора записей:
чтение с enable.auto.commit=true уже выполнено, затем ожидается отказ записи.
Это проверяет окно риска после чтения, но не доказывает реальный commit брокера.
E11 проверяет число записей каждого консьюмера, поля сообщения, сессии,
аргументы чтения, автофиксацию и выходной Загружено (регрессия R02).

Два новых замечания property-return-type в D04 устранены явным извлечением
message/key через Свойство с типизированными переменными. Совпадающий старый
хэш для подготовки берется из существующей функции, а ожидаемые контрольные
SHA256 тела abc и пустой строки заданы независимыми литералами. После всех
успешных изменений и регистрации E11 основной модуль снова имеет 197 замечаний.
Объект внешнего двойника имеет 31 замечание, перечисленные выше; чистая общая
диагностика не заявляется. Измененные методы проходят синтаксическую проверку EDT.

Всего в этом повторном запуске добавлено 107 регистраций поверх 2275:
расчетный итог 2382 (2380 основных + 2 A03). Последний успешный исходник основного
модуля: 13055 строк, contentHash 4b6688e86f26676b. Новые тесты не запускались;
обнаружение сценариев runner, общий прогон и покрытие не повторялись.

Попытка добавить H02 через write_module_source отклонена без изменения модуля:
source must contain exactly one complete Procedure/Function for mode=insertBefore;
found 2 declarations. Переданы вместе тест и callback; это ошибка формирования
вызова инструмента. Общий AGENTS.md, Evidence and Failure Gates, требует остановки
после отказа обязательного инструмента и нового пользовательского запуска.
H02 в этой попытке не добавлен и не зарегистрирован.

Для следующего запуска: подтвердить восстановленную готовность unit-edt, затем
добавить тест ВыборкаЛогов_ИсключаетТочнуюВерхнююГраницу и вспомогательный метод
ЗаписатьСообщенияНаГраницеЛогов ОТДЕЛЬНЫМИ insertBefore. В unit-заимствованном
модуле добавить минимальный Around для ОграничениеУказательПередВыгрузкойЛогов:
сначала ПродолжитьВызов(), затем однократный callback подготовки трех собственных
строк относительно полученной границы, затем вернуть неизмененную границу.
Это сохраняет исходные часы, расчет и запрос. Вызов только при маркере теста.
После каждой отдельной мутации дождаться сборки и выполнить focused diagnostics.

Продолжить оставшиеся ветви A02/A04/A10; C/D/F (блокировка между порциями,
сброс простоя и точные временные границы); D07 отказ записи регистра;
E10 удаление действующего распределения/сессий и сброс простоя;
сверить E11–E13 с уже добавленными тестами; G06/G08/G09, H02/H03, I05.
A10 немедленная авария не гарантируется падающим фоновым обработчиком;
жесткий Telegram-хост и встроенные часы требуют доказанной тестовой границы.
Не подменять внутренние вычисления указателей, не отправлять реальные уведомления.
После добавления всех достижимых сценариев — сверка матрицы, узкие прогоны,
общий прогон и покрытие. Полный план не завершен.

## H02, полный A04 и требования оформления — 11.09.2026

В новом пользовательском запуске подтверждены ready всех четырех проектов и
работа unit-edt:8768. H02 добавлен тремя отдельными методными изменениями:
ЗаписатьСообщенияНаГраницеЛогов, ВыборкаЛогов_ИсключаетТочнуюВерхнююГраницу и
unit-заимствованный Around ют_НаблюдатьГраницуВыгрузкиЛогов. Наблюдатель сначала
выполняет исходный расчет границы, затем при однократном маркере создает только
свои строки с метками граница-1, граница, граница+1 и возвращает исходное число.
Тест проверяет выборку только первой из этих строк и сохранность всех трех в БД.
Зарегистрировано два направления, тег ТочнаяГраницаЛогов.

ТаблицаРаспределения_УчитываетДиспетчерБлокировкиИУдаление дополнен прямым вызовом
всего ЗапуститьПотоки через существующий журнал границы запуска ФЗ. Существующие
параметрические сценарии теперь проверяют также отсутствие дубликатов запусков,
точные методы и лимиты, включение/исключение своих настроек и контрольной настройки.
Новое замечание invocation-parameter-type-intersect устранено явной аннотацией
типа контрольной ссылки. Основной модуль после последней проверки — 197 замечаний.
Заимствованный модуль после H02 — 389 замечаний (прежние перегруппированные
замечания 397 не воспроизвелись в этом выводе); чистая диагностика не заявляется.

Добавлен, но ЕЩЕ НЕ ЗАРЕГИСТРИРОВАН метод
ОбработатьДиспетчерЗадач_РазделяетСмешанныеБлокировки. Для него нужны четыре
сочетания Исходящие/СобственныйДиспетчер. Внутри четыре строки всех сочетаний
блокировок дают независимые пары настроек обработки и транспорта.

Расчетное число регистраций — 2384 (2382 основных + 2 A03), новые прогоны и
runtime discovery не выполнялись. Основной модуль: 13265 строк, hash
564faa8433cfe53d. Продукт не менялся. Все операции с src выполнены через EDT.

Новые обязательные требования пользователя:
1. Одна цепочка ЮТТесты в ИсполняемыеСценарии; ДобавитьТестовыйНабор вызывать
   последовательно, без повторного ЮТТесты и без промежуточных точек с запятой.
2. Все остальные методы основного тестового модуля поместить в предметные области
   по аналогии с ИдентификаторыОпераций. ИсполняемыеСценарии не группировать.
Эти изменения оформления пока НЕ ВЫПОЛНЕНЫ.

Попытка read_method_source для ИсполняемыеСценарии вернула [OUTPUT TRUNCATED]:
full size 193846 characters, maximum output size 100000 characters. Полный текст
не получен; неполный фрагмент не использован для изменения. По Evidence and
Failure Gates общего AGENTS.md работа остановлена до нового пользовательского
запуска. Для возобновления читать регистрацию через read_module_source небольшими
диапазонами (например, по 250 строк) и собирать полный метод в памяти, не на диске.
Не повторять read_method_source для этой большой процедуры.

Последний outline: 191 процедура, 39 функций. Регистрация на строках 6-3448.
Без предметной вложенной области остаются методы интерфейса до ФлагиИСпискиОпераций
(6237) и вспомогательные методы 11828-13003. Большая область ЗапускИПовторныйСтарт
(6564-10608) содержит разнородные группы; при оформлении желательно разделить ее
по предметам. Существующие ИдентификаторыОпераций, ОписаниеПравилОбмена,
ТекстЖурнала, УказателиПотоков, СтатусыУказателей, УказательУдаления, ХэшТела,
СдвигУказателя и вложенные области данных сохранить. Номера строк после следующих
мутаций изменятся: использовать свежие якоря и expectedHash.
После оформления зарегистрировать смешанные блокировки и продолжить весь остаток
исходного плана, не переходя к runtime до окончания добавления тестов.

## Отказ разбора EDT перед продолжением — 11.09.2026

Пользователь сообщил, что самостоятельно убрал повторные ЮТТесты, и поручил
добавить все оставшиеся тесты. В начале запуска get_server_status подтвердил
unit-edt:8768, list_projects — ready всех четырех проектов.

Обязательный get_module_structure для unit,
CommonModules/ОМ_кфкФоновыеОперацииСлужебный/Module.bsl завершился isError=true:
Mcp error -32603: Failed to execute runnable (java.lang.StackOverflowError).
Структура актуальной пользовательской версии не получена. Изменений src в этом
запуске не было. После отказа никаких обходных чтений через другие инструменты
или изменений по старому снимку не выполнялось.

Одновременно запрошенная диагностика двух основных модулей вернула 16 замечаний:
bsl-legacy-check-library-module на строке 20 основного тестового модуля:
«Данный модуль может содержать только процедуры и функции»; еще 15
 dynamic-access-method-not-found для ДобавитьСерверныйТест, Тег, СПараметрами
на строках 21-37. Это не чистая и не сопоставимая с прежними 197/389 диагностика;
причина не подтверждена без успешного разбора актуального исходника.

Работа остановлена по Evidence and Failure Gates общего AGENTS.md. Для
продолжения требуется восстановленная работа EDT и новый пользовательский
запуск. Не считать оставшиеся тесты добавленными. Последнее расчетное число
2384 относится к предыдущей контрольной точке и после пользовательского
редактирования заново не сверялось.
