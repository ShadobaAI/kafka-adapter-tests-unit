# кфкФоновыеОперацииСлужебный: план и чек-лист тестирования v2

Дата: 11.09.2026. Статус на 24.09.2026: реализация и приёмка набора выполнены по критерию пользователя; замер покрытия выполняет пользователь отдельно. BG19 оформлен как подтверждённое исключение из-за дефекта продукта.

Основа — контракты и риски из [плана v1](kfk-background-operations-test-plan.md). По уточнению пользователя, прежний набор примерно из 2500 тестов находится в спрятанном коммите с сообщением `kfk-background-operations-test-plan`. Это исторический пример избыточного объёма, а не исходный набор для миграции. Его поиск, восстановление, инвентаризация, матрица изменений и сравнение старых и новых прогонов не входят в задачу и не являются условием начала или приёмки реализации. Результат v2 оценивается по проверке контрактов на текущей ревизии, диагностике и фактическим прогонам нового набора. Указанные в v1 дефекты — сведения для проверки актуальных рисков, а прежние результаты не подтверждают новый набор.

## 1. Цель и изменение подхода

Защитить от потери сообщений, неверного подтверждения обработки, повреждения очередей и удаления нужных данных при приемлемой стоимости сопровождения. Единица планирования — наблюдаемый контракт и риск, а не каждый внутренний метод или комбинация параметров.

V2 заменяет требования v1 о самостоятельном тестировании каждого метода, применении всей матрицы ко всем методам группы и обязательных 100% строк. Реестр методов v1 остаётся навигационной картой. Независимые контракты, аварийные исходы и регрессии не исключаются ради уменьшения числа тестов.

- [x] Для каждого критичного контракта указать успешный исход, опасный отказ, существенную границу и проверяемое состояние.
- [x] Проверить все достижимые исходы решений, управляющих подтверждением, повтором, продвижением позиции и удалением данных. При составном решении показать независимое влияние условий и опасные взаимодействия.
- [x] Для остальной логики использовать классы эквивалентности; дополнительные тесты добавлять по выявленному риску или пробелу.
- [ ] Пользователь: измерить строки/ветви как средство поиска пробелов после закрытия набора; при выявлении пробела добавить проверку. Отсутствие замера не блокирует закрытие.

### Исходы и границы критичных контрактов P0

| Контракт | Успешный исход | Опасный отказ | Существенная граница | Проверяемое состояние |
|---|---|---|---|---|
| BG03 | Позиция проходит завершённый непрерывный префикс | Позиция перескакивает незавершённый пакет или чужой поток | Первый пакет незавершён, второй завершён; затем завершён первый | Позиция и назначения обоих потоков |
| BG04 | После перезапуска остаток назначается и завершается | Сообщение теряется или назначается повторно | Равные метки и исчезновение потока | Мультимножество ключей, назначения, конечная позиция |
| BG05 | Каждый исходный ключ попадает ровно в один пакет | Ключ пропущен или назначен дважды | Пусто, полный лимит, лимит плюс хвост | Кратность ключей в пакетах и назначениях |
| BG06 | Выбраны только доступные записи нужной операции | Чужая или занятая запись взята в обработку | Дата, статус, UUID, ключ и предел выборки | Выбранные ключи и неизменность контрольных строк |
| BG07 | Сериализация сохраняет согласованный успешный результат | Ошибка или отказ помечены успехом; назначение осталось | Пустое тело или отсутствие правила | Статус, тело, журнал, счётчик, назначение |
| BG08 | Два сообщения передают обработчику собственные данные | Данные первого сообщения переходят во второе или ошибка игнорируется | Успех, отказ, пропуск и ошибка регистра | Переданные key/headers/тело, статусы, остановка регистра |
| BG09 | Ответ транспорта сопоставлен своему сообщению | Ошибка записана на другой ключ | Пропуск перед успехом и ошибкой в одном пакете | Статусы по каждому исходному ключу |
| BG10 | После отказа отправка доступна повторно | Ранний хэш подавляет повтор или успех дублируется | Общий и частичный отказ, исключение, успешный дубль | Очередь, хэши, повторная доступность |
| BG11 | Сбой записи не считается успешным завершением | Транспортный успех скрывает ошибку сохранения | Ошибка записи после положительного ответа | Очередь, назначения, хэши, повторная доступность |
| BG12 | Полученное сообщение сохраняет идентичность и содержимое | Пустой и отсутствующий key смешаны или payload искажён | Отсутствующий key против пустого; двоичное тело | Строка ИБ, все поля сообщения, `Загружено` |
| BG13 | Ошибка записи после чтения явно возвращается | `Загружено` истинно без строки ИБ | Чтение с автофиксацией и отказ записи | Отсутствие строки, `Загружено = Ложь`, ошибка |
| BG14 | После успешной обработки дубль подавлен | После ошибки дубль потерян или после успеха обработан снова | Успех против ошибки предыдущей обработки | БД, локальный кэш, число обработок |
| BG17 | Повтор и отмена касаются только допустимых записей | Чужая или новая запись изменена | Предел попыток, возраст, статус, занятость | Статусы выбранных и контрольных записей |
| BG18 | Очистка удаляет только устаревшие завершённые данные | Удалена актуальная, незавершённая или чужая строка | До/на/после порога; сроки -2, -1, 0 и дробный; полный пакет и хвост | Состав входящей и исходящей очередей, позиции логов |
| BG19 | Позиция лога растёт только после успешной отправки | HTTP-ошибка подтверждена или равные метки оставили хвост | Равные метки через границу порции; HTTP 500 и исключение | Позиция лога и оставшиеся события |
| BG22 | Новый контейнер начинает с чистого состояния | Старые строки, отбор или метка попадают в новый вызов | Два последовательных вызова с разными наборами | Очередь, журнал, метки и SHA256 |
| BG23 | Транспорт получает точную адресацию и payload | Неверный key/topic/partition или запись без topic отправлена | Пустой и заданный key; отсутствующий topic | Аргументы транспортной границы и `ОшибкаВыгрузки` |

### Карта решений, влияющих на подтверждение и удаление

| Решение | Независимые исходы и опасное сочетание | Проверка |
|---|---|---|
| Позиция пакетов (BG03–BG04) | Два статуса завершения: 00, 01, 10, 11; пустая выборка до/на/после позиции; чужой и исчезнувший поток, равные метки | `ОбновитьУказательНачалаВыборки_УчитываетПрефикс`, `ОбновитьУказательНачалаВыборки_ПустаяВыборкаНеОткатываетПозицию` и сценарии перезапуска |
| Исход сериализации (BG07) | Успех, явный отказ, исключение, отсутствующие тело/правило/метод дают разные статусы; назначение освобождается при каждом исходе | `Сериализация_СохраняетСогласованныйИсход` |
| Подтверждение отправки (BG09–BG11) | Успех, общий отказ, частичный отказ, исключение и ошибка записи; пропуск перед ответами пакета проверяет соответствие индексов; успешный дубль подавляется отдельно | `Отправка_СопоставляетОшибкуПослеПропуска`, `Отправка_РазрешаетПовторПослеОшибки`, `Отправка_ЧастичнаяОшибкаНеПодтверждаетХэш`, `Отправка_ИсключениеНеПодтверждаетСообщение`, `Отправка_ОшибкаСохраненияНеПодтверждаетЗавершение`, `Отправка_ПодавляетУспешныйДубль` |
| Подтверждение получения и хэш (BG12–BG14) | Успешная/ошибочная первая обработка независимо от включённой/выключенной идемпотентности; отказ записи после чтения с автофиксацией | `ОбработатьВходящееСообщение_ХэшПоявляетсяТолькоПослеУспеха`, `ОбработатьВходящееСообщение_ОтключеннаяИдемпотентностьНеПодавляетПовтор`, `ОбработчикЗагрузкиПакетСообщений_ОшибкаЗаписиНеСохраняетПакет` |
| Возврат и отмена (BG17) | До/на/после предела попыток и возраста; успешный статус, занятый ключ и новая регистрация исключаются независимо | `ВыборкаВозвратаВОчередь_ПроверяетГраницуИПопытки`, `ВыборкаВозвратаВОчередь_НеВозвращаетУспешныеСообщения`, `ВыборкаВозвратаВОчередь_СохраняетЗанятыйКлюч`, `ВыборкаВозвратаВОчередь_СохраняетНовуюРегистрацию`, проверки отмены дублей |
| Удаление (BG18) | Срок -1 отключает удаление; -2 зависит от наличия позиции логов; 0, положительный и дробный срок выбирают минимум времени и позиции логов до/на/после границы; -3 вызывает исключение; статус, направление, полный пакет и хвост проверены отдельно | `РассчитатьУказательУдаления_УчитываетРежим`, `РассчитатьУказательУдаления_СоблюдаетВременнуюГраницу`, `РассчитатьУказательУдаления_ОтклоняетНекорректныйСрок`, `ВыборкаУстаревшихСообщений_ПрименяетСрокКСвоемуСтатусу`, `УдалитьЗаписиРС_УдаляетПолныйПакетИХвост` |
| Позиция логов (BG19) | Ответ 200 продвигает позицию; 500 и разрыв удерживают её в обоих направлениях. Равные метки на границе порции обнаружили известную потерю хвоста | `Логирование_СохраняетСобытие`, `Логирование_ОшибкаУдерживаетПозицию`; `Логирование_НеПерескакиваетРавныеМетки` временно закомментирован по прямому указанию пользователя после результата 0/2. Дефект остаётся открытым для продукта; согласованное исключение не блокирует закрытие пункта |

### Классы эквивалентности остальной логики

| Контракты | Представители классов | Риск, вызвавший дополнительную границу |
|---|---|---|
| BG01–BG02 | Опции включены/выключены, блокировка есть/нет; для четырёх независимых флагов — базовый случай, одиночные включения и совместное включение | Повторный запуск активного задания и сочетание флагов |
| BG15–BG16 | Пустой вход, нагрузка до/на/после порога, предел числа потоков; блокировка до/между порциями, пустой цикл и поступление данных | Межсеансовое назначение и освобождение только ненужных сессий |
| BG20–BG21 | Один эталон события каждого направления, отдельные классы заголовков/тела; SLA до/на/после порога, оба направления и повторный запуск | Ошибка единиц времени и смешение направления |
| BG24 | Одинаковый состав настроек в разном порядке и другой состав | Нестабильный раздел операции |

Дополнительный тест равных меток BG19 возник при сверке опасного взаимодействия. Он выявил дефект продукта и временно исключён из регистрации по прямому указанию пользователя; это не добавляет новых классов к второстепенной логике.

Измерение покрытия строк и ветвей выполняет пользователь отдельно; отсутствие замера не ограничивает закрытие пунктов и приёмку набора. Известный дефект продукта BG19 оформлен согласованным исключением: регистрация воспроизводящего теста закомментирована до исправления продукта.

## 2. Паттерны и их применение

Это адаптация подходов из других экосистем, а не предложение подключить Python/Java/C++ к запуску 1С.

| Паттерн | Применение в этом плане | Ограничение |
|---|---|---|
| Arrange–Act–Assert; один наблюдаемый результат | Подготовка очереди → одна операция → согласованный результат в очереди, назначении и позиции | Несколько утверждений одного контракта допустимы; несвязанные операции не объединять |
| Table-driven tests, как параметризация pytest | Именованные строки таблицы для одной семантики: статус задания, граница времени, ответ транспорта | Каждая строка считается отдельным запуском; таблица не превращается в декартово произведение |
| Sociable unit tests | Проверять алгоритм с настоящими внутренними помощниками; изолировать внешнюю границу | SQL, транзакции и межсеансовые эффекты требуют отдельных проверок на платформе |
| State-based testing вместо избыточных mocks | Проверять сохранённые сообщения, позицию, хэши и неизменность чужих данных | Кратность/порядок вызовов проверять, когда это контракт: запрет повторной отправки, закрытие ресурса |
| Test Data Builder | Небольшие смысловые конструкторы сообщения, очереди и указателя; значимые значения видны в тесте | Конструктор не запускает проверяемую операцию и не вычисляет за неё ожидаемый результат |
| Model-based/stateful testing | Короткая последовательность «назначить → завершить второй → перезапустить → завершить первый» с инвариантом позиции | Модель описывает множество незавершённых работ, не копирует алгоритм продукта |
| Property/metamorphic testing | Перестановка порядка завершения и изменение разбиения на порции сохраняют итоговый набор обработанных ключей | Только при одинаковых предусловиях и без изменения семантики дубликатов; фиксированный воспроизводимый корпус |
| Fault injection | Ошибка транспорта и ошибка сохранения на разных границах подтверждения | Не подменять внутренний алгоритм, состояние которого проверяется |
| Mutation testing как проверка силы теста | Для опасного решения назвать изменение, которое обязан обнаружить тест: `>` вместо `>=`, чужой ID позиции, раннее сохранение хэша | Мысленный разбор не равен выполненному mutation testing; изменение продукта этим планом не разрешено |

Источники: [pytest: параметризация](https://docs.pytest.org/en/stable/how-to/parametrize.html), [Martin Fowler: Unit Test](https://martinfowler.com/bliki/UnitTest.html), [GoogleTest: gMock Cookbook](https://google.github.io/googletest/gmock_cook_book.html), [Hypothesis: stateful tests](https://hypothesis.readthedocs.io/en/latest/stateful.html), [PIT: mutation testing](https://pitest.org/). Конкретные границы и приоритеты ниже — проектные решения v2.

## 3. Размер и организация набора

Три уровня проверок, выбираемые по контракту. Разделение на файлы само по себе не является сокращением.

| Уровень | Что проверяется | Зависимости |
|---|---|---|
| Быстрые модульные | Указатели, пакеты, решения, распределение, преобразование сообщений | Прямые вызовы, управляемые входы, подмена только внешних границ |
| Компонентные с ИБ | Реальные запросы, отборы, сохранение статусов, хэшей, очистка | Реальная тестовая ИБ, изоляция каждого теста; категория `Модульные`, дополнительный тег `Компонентные` |
| Стендовые | Межсеансовая видимость, конкуренция, реальные ФЗ и транспортные соглашения | Серверная тестовая ИБ и явная очистка; категория `Модульные`, дополнительный тег `Стендовые` |

Предварительный ориентир — 120–180 именованных запусков основного регрессионного набора, включая строки параметров и стендовые случаи. Это гипотеза планирования, не достигнутый результат и не запрет на необходимую проверку. При превышении 180 сначала пересмотреть повторения и фикстуры; сохранить обоснованные уникальные критичные случаи. Окончательный бюджет установить после пилота по позиции и отправке.

- [x] Считать отдельно процедуры, параметризованные запуски, примеры генерации, длительность и объём фикстур.
Учёт набора, 24.09.2026: в пяти модулях 81 тестовая процедура; 38 из них параметризованы. Зарегистрировано 144 строки параметров и 43 одиночных запуска, всего 187 запусков. Генеративных примеров в этом наборе нет (0); все входы заданы явно. Полный прогон того же задания: 187/187, 0 ошибок, 0 пропусков, около 340 с от запуска до результата. Основную длительность задаёт стендовый тест штатного пятиминутного таймаута пустого потока; его ожидание 290–320 с задано в тесте. По проверенным фикстурам: распределение пакетов использует до 7 синтетических ключей за вариант, девять вариантов проверки срока создают по 2 граничные записи, стендовый сценарий — до 2 входящих сообщений и одного ФЗ. Ориентир 180 превышен на 7 запусков; обзор регистрации приведён в следующем пункте.

- [x] Для регистрации с более чем 8 вариантами провести краткую проверку: какую отдельную ошибку ловит каждая строка. Число 8 — сигнал ревью, не ограничение покрытия.

Ревью 9 вариантов: единственная регистрация выше порога — ВыборкаУстаревшихСообщений_ПрименяетСрокКСвоемуСтатусу. Шесть исходящих пар проверяют отдельную маршрутизацию срока для Новое и Обработано (общий срок незавершённых, но разные статусы), ОшибкаОбработки, ОбработкаОтменена, ВыгрузкаОтменена и ОшибкаВыгрузки. Три входящих пары проверяют Новое, ОшибкаОбработки и ОбработкаОтменена; одноимённые статусы разных направлений используют разные настройки. Каждый вариант создаёт запись до порога и на пороге и ловит ошибку выбора срока или статуса для своей пары. Повторяющихся пар нет; девять вариантов сохранены.
- [x] Вынести повторяющуюся подготовку в небольшие помощники; не создавать универсальный сценарий с десятками переключателей.

Ревью подготовки: пять модулей используют локальные помощники по предмету проверки — НовыйСценарийПозиции с двумя статусами, НовыйПакет для транспортной границы, НовыйСценарийДедупликации и НоваяЗаписьОчереди для входящих/очистки, ЗапуститьТестовоеЗадание и сигналы для стенда. Ожидаемые результаты остаются в тестах или отдельных проверяющих помощниках. Универсального сценария с множеством переключателей в этих модулях нет; дополнительное объединение разных фикстур не требуется.
- [x] Группировать наборы по контрактам из раздела 5. Точные имена/размещение модулей выбрать по текущему владельцу и правилам YAxUnit перед реализацией.

Группировка сверена с разделом 5 и текущим владельцем unit: основной модуль хранит позицию, пакеты, повторное использование и логи; отдельные модули — отправку, получение/очистку, управление и реальный фоновый стенд. Внутри них регистрации разделены тестовыми наборами по поведению, включая получение, дедупликацию, повторы, очистку, SLA, отправку и стендовый цикл. Все пять модулей вошли в успешный полный прогон 187/187.
- [x] Использовать русские теги категории, объекта, операции и поведения согласно README; уровень проверки обозначать тегами Компонентные/Стендовые, без тегов важности.

Ревью тегов, 24.09.2026: по уточнению пользователя во всём наборе фоновых операций используется категория Модульные, поскольку внешние ресурсы не задействованы; реальные ФЗ дополнительно отмечены как Стендовые, проверки с ИБ — как Компонентные. В регистрациях Управление, Отправка и ПолучениеОчистка добавлены точные русские теги операций, в Управление — теги поведения. По уточнению пользователя удалены все 28 тегов Важные/Критичные из пяти модулей. Диагностика после изменений: 0 замечаний. Целевые запуски тегов и изменённых тестов SLA: 4/4 и 12/12; после возврата категории Модульные стендовые проверки прошли 3/3 и 1/1. Три замечания конструктора структуры в тестах SLA, обнаруженные пересчётом EDT, исправлены. Один запуск с неверным форматом фильтра tests завис и остановлен без отчёта; его результат не учитывается.
- [x] Не создавать отдельные тесты для простой константы или делегирующего метода, если её ошибочное значение уже обнаруживается тестом контракта. Внешние UUID и таблицы соответствий проверить компактной таблицей там, где они самостоятельный контракт.

Ревью 81 зарегистрированной процедуры: отдельные проверки простой константы или пустого делегирования в наборе фоновых операций не найдены. Самостоятельные значения проверяются компактно по контракту: SHA256 — пять эталонных входов, состав операций по четырём флагам — шесть различающих строк; адресация Kafka — два варианта key и отсутствие topic. Самостоятельного внешнего UUID-контракта в BG01–BG24 нет.

## 4. Правила отбора входов

1. Выбрать минимальный нормальный вход.
2. Для каждого условия найти вход, меняющий существенный результат. Неприменимые комбинации исключить с причиной.
3. Добавить границы реального решения: до/на/после порога. Не размножать одну границу по всем операциям при общей реализации.
4. Добавить известное опасное взаимодействие: пропущенная строка перед ошибкой транспорта, незавершённая порция перед завершённой, равные временные метки на границе пакета.
5. Сопоставить строки с контрактами. Повторы удалить только при сохранении утверждений и уровня проверки.

Пример A07 из v1: при подтверждённой независимости четырёх флагов достаточно базового состояния, четырёх одиночных включений и совместного включения как проверки объединения результата. Условия логирования/контроля проверяются отдельной компактной таблицей; их не перемножают автоматически с этими шестью входами. Если порядок, приоритет или взаимодействие флагов влияет на результат, соответствующие комбинации обязательны. Проверка независимости по актуальной реализации — подготовительный шаг, а не допущение о продукте.

Попарный отбор допустим для второстепенных настроек после анализа ограничений. Он не заменяет тесты отказов, временных последовательностей и сочетаний, управляющих потерей данных. Для малого критичного решения полный перебор четырёх булевых комбинаций может быть проще и надёжнее сокращения.

## 5. Основной чек-лист контрактов

Каждая строка — ограниченное семейство одного поведения. Перечисленные разные исходы регистрируются отдельно; перечисленные поля одного результата проверяются вместе. Не применять всю строку ко всем методам группы v1.

| Готово | ID / приоритет | Контракт и минимальные различающие случаи | Наблюдаемое доказательство | Основа v1 |
|---|---|---|---|---|
| [x] | BG01 / P1 | Запуск разрешён только при нужных опциях и отсутствии блокировки; активное задание переиспользуется | Правильные адресат/операция/лимит; нет второго запуска при повторе | A01–A06, A09–A10 |
| [x] | BG02 / P1 | Служебные операции соответствуют независимым флагам и опциям | Точный состав операций, обязательная очистка, отсутствие лишних | A07–A08, A11–A13 |
| [x] | BG03 / P0 | Позиция продвигается только за непрерывно завершённым префиксом | Незавершённый первый пакет удерживает позицию; его завершение разрешает продвижение; чужой поток не влияет | B06–B08, B11 |
| [x] | BG04 / P0 | Перезапуск и исчезновение потока не теряют остаток | Ключи до/после перезапуска, оставшиеся назначения и конечная позиция; равные метки | B01–B04, B11 |
| [x] | BG05 / P0 | Разбиение на пакеты не теряет и не размножает ключи | Пустой вход, полный пакет, хвост, границы фактического лимита; мультимножество ключей и кратность назначения | B09–B10 |
| [x] | BG06 / P0 | Выборка берёт только доступную запись нужной операции | Контрольные строки с чужим владельцем, статусом, UUID, датой и занятым ключом остаются нетронутыми; порядок/лимит | C03–C04, D02, F01 |
| [x] | BG07 / P0 | Сериализация фиксирует согласованный исход сообщения | Успех, отказ, ошибка, отсутствие тела/правила: статус, тело, журнал, счётчик и освобождение назначения | C05–C09 |
| [x] | BG08 / P0 | Граница КД/произвольного обработчика передаёт правильные данные без утечки между сообщениями | Два разных сообщения подряд, разные ключи/заголовки; флаги успеха/ошибки/пропуска; остановка регистра на ошибке | C10–C12, F02, F04, F06–F07 |
| [x] | BG09 / P0 | Ошибка ответа транспорта относится к исходному сообщению | Пакет: пропуск → успех → ошибка; проверить статусы по ключам, а не только число ошибок | D03, D05, R06 |
| [x] | BG10 / P0 | Неуспешная отправка остаётся доступной для повтора | Общая/частичная ошибка, исключение; хэш не подавляет повтор; успешный дубль подавляется в разрешённом режиме | D04, D06–D07, R07 |
| [x] | BG11 / P0 | Сбой сохранения не маскируется успешным завершением | Ошибка записи после транспортного ответа; фактические очередь, назначения, хэши и возможность повтора | D07 |
| [x] | BG12 / P0 | Входящее сообщение сохраняется без искажения идентичности и полезной нагрузки | Отсутствующий и пустой key различаются; headers, двоичное тело, topic/partition/offset/timestamp сохранены; Загружено согласован с результатом | E11–E12, R02 |
| [x] | BG13 / P0 | Ошибка записи после чтения с автофиксацией имеет явно установленную гарантию | Чтение вызывается с автофиксацией; при отказе записи нет строки в ИБ, Загружено = Ложь и ошибка не скрыта. Kafka offset может продвинуться независимо от ИБ; это принятое ограничение режима | E13 |
| [x] | BG14 / P0 | Дедупликация получения учитывает только успешную обработку | Дубль после успеха подавлен; дубль после ошибки допускается; БД и локальный кэш согласованы | F03–F05 |
| [x] | BG15 / P1 | Кэш и распределение консьюмеров сохраняют рабочее состояние | Повторная инициализация, добавление/удаление консьюмера, порог нагрузки и лимит потоков; не смешаны адреса | E01–E08 |
| [x] | BG16 / P1 | Цикл корректно останавливается и освобождает ресурсы | Блокировка до/между порциями, пустой вход, таймаут, поступление данных; закрыты только ненужные сессии | C01–C02, D01, E09–E11 |
| [x] | BG17 / P0 | Повторная отправка и отмена дублей выбирают только допустимые записи | Предел попыток, возраст, статус, занятость; новая регистрация и чужие записи сохраняются | G01–G03 |
| [x] | BG18 / P0 | Очистка не удаляет актуальные/незавершённые/чужие данные | До/на/после порога; срок -1 отключает удаление; -2 использует позицию логов, без неё удаление отключено; срок >= 0 задаёт временную границу, при наличии позиции логов выбирается меньшая (позиция до/на/после границы); 0, положительный и дробный срок; некорректный -3 вызывает исключение; разные входящая/исходящая позиции; полный пакет и хвост | G04–G09, R04 |
| [x] | BG19 / P0 | Логирование не перескакивает хвост и не подтверждает ошибочную отправку | Одинаковые метки через границу порции; HTTP-ошибка и исключение удерживают позицию; успех продвигает | H02–H03, H09, R09 |
| [x] | BG20 / P1 | Событие лога соответствует внешнему контракту | По одному эталону входящего/исходящего события; отдельные варианты заголовков/тела, путь POST, gzip и JSON | H01, H04–H09 |
| [x] | BG21 / P1 | Контроль SLA использует нужное направление и единицы времени | Разные диапазоны входящих/исходящих, граница длительности, повторный запуск; состав показателей и сообщения | I01–I05, R05 |
| [x] | BG22 / P0 | Повторное использование контейнера не переносит старые данные | Два вызова с различными наборами; журнал/очередь/метки не содержат остатка; SHA256 по независимому эталону | C13, F08, I06–I08, R08, R10 |
| [x] | BG23 / P0 | Исходящее Kafka-сообщение сохраняет адресацию и полезную нагрузку | На границе транспорта сверить независимый эталон: правильный топик, пустой КлючСообщения заменяется значением КлючОбъекта, заданный сохраняется; partition = -1 в запросе, тело и headers без искажений; при отсутствии топика запись получает ОшибкаВыгрузки и не попадает в отправляемый массив. BG-S03 проверяет передачу на подменённую границу компоненты без фактической доставки | D02–D03 |
| [x] | BG24 / P1 | Раздел операции стабилен при перестановке настроек и различает их состав | Прямой вызов ПолучитьРазделОперации с двумя различными настройками и их перестановкой даёт одинаковое Значение; замена одной настройки другой меняет Значение. Ожидаемое отношение результатов не вычислять копией алгоритма SHA256 | B05 |

P0 — риск потери/повреждения данных или ложного подтверждения; P1 — остальные эксплуатационные контракты. P1 входит в полную регрессию и не означает «можно не реализовывать».

### Дополнительные проверки BG-S

- [x] BG-S01: два реальных ФЗ с контролируемым обратным завершением; проверить межсеансовую видимость назначений и позиции. Управлять порядком через согласованные сигналы, а не случайные паузы.
- [x] BG-S02: аварийное завершение/отмена реального ФЗ и повторный запуск; проверить сохранение остатка, отсутствие лишнего задания и очистку тестового окружения.
- [x] BG-S03: на подменённой границе Kafka-компоненты проверить topic, key, partition = -1, тело, headers и успешный ответ отправки; при ошибке записи после чтения с автофиксацией проверить ошибку, отсутствие записи в ИБ и Загружено = Ложь. По согласованному ограничению внешняя Kafka не используется; успешный ответ мока считается подтверждением отправки на границе компоненты, а фактическая доставка брокеру не заявляется.
- [x] BG-S04: тесты с моком проверяют формирование POST, gzip-тело, события и обработку ответа 500, а также Telegram. По согласованному допущению ответ мока 200 означает успешный приём сервисом; внешние скрипты и ресурсы не используются.

BG-S03 / BG13, 2026-09-23: исходные стендовые сценарии с Kafka удалены из набора по требованию не использовать внешние ресурсы. `ПакетKafka_СохраняетАдресациюИТело` проверяет на подменённой границе компоненты два варианта key, topic, partition, тело, headers и успешный ответ (2/2). `ОбработчикЗагрузкиПакетСообщений_ОшибкаЗаписиНеСохраняетПакет` проверяет чтение с автофиксацией, отказ записи и состояние ИБ (1/1). Весь BG-S03 прошёл 3/3 без брокера. По принятой модели ответ мока считается подтверждением передачи на границу; фактическая доставка и offset брокера не проверяются. Автофиксация выбрана ради производительности, возможность продвижения offset при ошибке ИБ зафиксирована как ограничение. BG13 и BG-S03 завершены в согласованной модели.

BG-S04, 2026-09-23: тест с подставленной HTTP-сессией проверил POST, путь, заголовки, события в gzip-теле, успешный ответ 200 и обработку ответа 500 (1/1). По согласованному с пользователем допущению ответ мока 200 считается успешным приёмом сервисом. Telegram проверен с подменой HTTP-сессии (1/1). Реальный сетевой обмен и независимое декодирование gzip не проверялись; внешние скрипты и ресурсы не используются. Пункт завершён в согласованной модели.

BG06, 2026-09-23: входящие выборки до распределения и в потоке проверены тестами `ВыборкаДесериализации_ОтбираетСвободныеВПорядкеМетки` и `ВыборкаДесериализацииВПотоке_УчитываетКлючДатуИПоток` (2/2). Проверены владелец, статус, граница метки, занятость, полный ключ назначения (ключ записи + дата + UUID потока), порядок и лимит. Пункт завершён.

BG08, 2026-09-23: `Десериализация_ДваСообщенияНеСмешиваютСвойства` прошёл 1/1. Внешний обработчик получил два разных тела, ключа и заголовка; добавленный в первом сообщении заголовок не попал во второе. Проверены статусы после успеха и отказа. Совместно с существующими проверками сериализации и ошибки десериализации пункт завершён.

BG16, 2026-09-23: реальные фоновые задания проверили блокировку до порции, блокировку между порциями и поступление второй записи во время цикла (3/3). Отдельный пустой поток завершился по штатному таймауту 300 секунд (1/1). Ранее проверены пустая интерактивная выборка и закрытие только удалённых сессий. Пункт завершён.

BG21, 2026-09-23: прежний результат 3/5 относился к ошибке в запросе завершённых входящих: вместо `&МеткаИзмененияДоВх` использовалось `&МеткаИзмененияДоИсх`. В текущем исходнике обе ветви входящих используют `&МеткаИзмененияДоВх`; дефект диапазона больше не воспроизводится. Проверены четыре показателя, сообщение и повторный запуск контроля. Набор BG21 прошёл 8/8; пункт закрыт.

BG22, 2026-09-23: `НоваяОчередь_ОчищаетСтрокиИОтбор` усилен явной старой меткой и проверкой начальной метки новой строки до записи и в ИБ; входящий и исходящий варианты прошли 2/2. С учётом ранее проверенных SHA256, журнала и очистки очередей пункт завершён.

BG23, 2026-09-23: пакет на подменённой границе отправки и отсутствие топика проверены 3/3 по topic, key, partition = -1, телу и headers. BG23 завершён; BG-S03 дополнительно закрепляет согласованную проверку отправки и ошибки приёма без внешней Kafka.

BG19, 2026-09-24: пункт выполнен успешно в согласованном пользователем режиме. Тест `Логирование_НеПерескакиваетРавныеМетки` для входящих и исходящих логов был зарегистрирован и запущен: 0/2, оба варианта подтвердили известный дефект продукта — после первой порции указатель принимает общую метку, а следующая выборка с условием `МеткаИзмененияМС > НачальнаяПозиция` не возвращает оставшееся событие. По требованию пользователя четыре строки регистрации теста закомментированы; тело теста сохранено. После исправления продукта раскомментировать регистрацию и повторить оба запуска. Диагностика `unit-edt` до и после изменения регистрации: 0 замечаний. Соседний набор `Логирование_ОшибкаУдерживаетПозицию` после комментирования прошёл 4/4. Успешное выполнение пункта означает оформление согласованного исключения, а не прохождение теста или устранение дефекта.

## 6. Фикстуры, изоляция и ожидаемые результаты

- [x] Зафиксировать владельца `unit`, серверный контекст и актуальные точки входа через назначенные инструменты. Исходники `src/**` читать/менять только разрешёнными MCP; изменения — `unit-edt:8768`.

Проверено через unit-edt, 24.09.2026: проект unit открыт и готов как расширение; тестовая ИБ Адаптер Kafka - UNIT наследуется от base. Пять модулей ОМ_кфкФоновыеОперацииСлужебный* принадлежат unit, включены для серверного контекста и регистрируют проверки через ИсполняемыеСценарии(). Проверяемый кфкФоновыеОперацииСлужебный находится в собранном base; в unit есть расширяющий модуль с точками ют_ для тестов. Изменения src выполнены через unit-edt, текущие модули сверены через его live-модель.
- [x] До реализации сверить существующие фикстуры через `kfk-unit`; продуктовые зависимости — по разрешённым `kfk`/`kfk-base`. Этот документ не подтверждает текущий состав исходников.

Сверка 24.09.2026: kfk-unit показывает локальные предметные помощники пяти тестовых модулей (позиция, пакет, записи очередей, дедупликация, сигналы стенда). Продуктовая процедура ОбновитьУказательНачалаВыборки найдена в kfk; kfk-base по этому точному имени результата не дал. Актуальное состояние тестов и модуля продукта при изменениях проверено в unit-edt; индекс использован только как дополнительная навигация.
- [x] Создавать данные через подходящие публичные средства `ЮТест.Данные()`; значимые ключи, даты и статусы задавать явно и согласованно.

Сверено в актуальных помощниках: НовыйПродюсер и НовыйКонсьюмер создают элементы справочников через ЮТест.Данные().КонструкторОбъекта; для регистров с проверяемыми граничными полями НоваяЗаписьОчереди и ЗаписатьСообщениеСтенда явно задают собственный ключ, метку, статус и связанные поля через менеджер записи. Такой способ сохраняет видимые в тесте границы и идентичность записей.
- [x] Каждый тест владеет своими записями. Подготовка/очистка по умолчанию на уровне теста; общими могут быть неизменяемые описания, а не изменяемая очередь.

Изоляция сверена: ключи сообщений создаются как UUID; обычные регистрации используют транзакцию теста, а ПослеКаждогоТеста сбрасывает моки и очищает собственные ключи после нетранзакционных отказов. Стендовый цикл хранит свои два ключа и ФЗ в контексте теста, управление удаляет назначения и сигналы текущего запуска. Общей изменяемой очереди между тестами нет.
- [x] Состояние ИБ проверять через `ЮТест.ОжидаетЧтоТаблицаБазы(...)` и специализированные утверждения ИБ. В результат включать контрольные чужие строки.

В пяти модулях найдено 42 обращения к ОжидаетЧтоТаблицаБазы. Проверки позиции дополнительно сверяют неизменность записи чужой операции; выборки и очистка используют контрольные чужие ключи/статусы, отправка — записи очереди, назначений и хэшей. Утверждения сверены с сохранённым состоянием ИБ, а не только с возвращаемыми значениями.
- [x] Транзакционный откат выбирать только для кода, полностью выполняемого в этой транзакции. Реальные ФЗ, commit и внешние эффекты проверять вне обёртки с явной очисткой; автоматическое удаление созданных данных не восстанавливает произвольные изменения.

Проверено в регистрациях: обычные тесты используют ВТранзакции(), реальные ФЗ и сценарии отказа сохранения явно задают ВТранзакции(Ложь). Для них есть собственная очистка после теста: остановка ФЗ, удаление назначений/сигналов и адресная очистка очереди, хэшей и контекста по тестовому ключу.
- [x] Управлять временем/транспортом только через существующие допустимые границы. Если необходимой границы нет, зафиксировать препятствие; этот план не разрешает менять продукт или платформенные методы.

Для логов фиксируется только существующая граница ОграничениеУказательПередВыгрузкойЛогов; тестовые записи получают явные миллисекундные метки. Kafka и HTTP подменяются на внешних границах компоненты/сессии. Порядок реальных ФЗ задают UUID-сигналы с ограниченным ожиданием, а не случайная задержка. Недоступность исправления равных меток BG19 оформлена как дефект продукта, без изменения продукта в этой задаче.
- [x] Не копировать проверяемую формулу в ожидаемый результат. Для позиции использовать вручную разобранные последовательности; для пакетов — исходное мультимножество ключей; для внешнего сообщения — независимый эталон.

Сверено: позиции и размеры пакетов перечислены литеральными ожидаемыми строками параметров; ПроверитьСценарийПозиции получает отдельную ожидаемую метку, а не вычисляет её алгоритмом продукта. Пакетный тест сопоставляет ключи исходной выборки с результатом, внешние события и SHA256 — с независимыми эталонами.
- [x] Не мокировать расчёт указателя, SQL-отбор или сохранение хэша в тесте именно этого поведения. Для BG-S03 проверять данные и ответ на подменённой транспортной границе, не заявляя фактическую доставку брокеру.

Проверенные тесты вызывают реальные методы расчёта позиции, запросов выборки и записи хэша. Подменяются границы компоненты Kafka, HTTP и часы отбора логов. BG-S03 проверяет topic, key, partition, тело, headers и ответ подменённой компоненты; доставка брокеру в результатах не заявлена.
- [x] После ошибки восстанавливать mocks, контекст, настройки, записи и внешние ресурсы; проверять самостоятельный и повторный запуск.

ПослеКаждогоТеста в основном, отправочном и принимающем модулях сбрасывает Мокито и адресно очищает контекст/записи после отказа. Стендовые процедуры останавливают собственные ФЗ и восстанавливают блокировку/сигналы. Отдельные BG13/BG19/BG21 и стендовые сценарии запускались целево, затем зарегистрированный набор прошёл общим прогоном 187/187; изменённые регистрации дополнительно прошли 4/4, 12/12, 3/3 и 1/1.

## 7. Реализация нового набора

Набор строится по контрактам BG01–BG24 и BG-S01–BG-S04 на текущей реализации продукта. Отсутствие прежних тестов в рабочей ревизии не блокирует работу. Не воспроизводить избыточный перебор параметров, в том числе в скрытых циклах.

- [x] Зафиксировать ревизию проверяемого продукта и нового набора.

Ревизии на 24.09.2026: adapter/adapter dc3c6740ddf429782ec608653aa8b24c9d6567fb; adapter/base af62a80f6be3abd949c20268734c8fcb3a99dbc5; оба рабочих дерева продукта чистые. tests/unit/unit: HEAD 928e48eaeff46ef6059592f3bc0a7e47e9c6a785 плюс текущие незакоммиченные изменения плана и пяти тестовых модулей. Прогоны и диагностика этого хода относятся к указанным ревизиям продукта и текущему состоянию тестового дерева.
- [x] Для каждого контракта подобрать различающие входы, наблюдаемые утверждения и необходимый уровень проверки.

Для BG01–BG24 и BG-S01–BG-S04 входы и наблюдаемое состояние перечислены в разделе 5 и матрице P0 раздела 1. Различающие границы включают полный пакет и хвост, до/на/после срока, успешный/ошибочный транспорт, пустой и заданный key, статус и направление записи. Уровень указан по проверяемому контракту: прямой BSL, сохранённое состояние ИБ, реальные ФЗ либо подменённая внешняя граница.
- [x] Сначала реализовать BG03–BG05, BG09–BG11 и BG23–BG24; по фактическим прогонам оценить достаточность проверок, длительность и объём подготовки.

Пилот подтверждён текущим набором: позиция и пакеты BG03–BG05, отправка BG09–BG11, адресация BG23 и стабильность раздела BG24 зарегистрированы и прошли в полном прогоне 187/187. Различающие тесты включают 4 состояния двух пакетов, пустой/полный/хвостовой пакет, пропуск перед ошибкой, отказ и повтор, отсутствие topic и два варианта key. Длительность и объём подготовки измерены в разделе 3.
- [x] Выполнить новые затронутые тесты в изолированных запусках; разобрать падения и проверить повторный запуск.

После правок регистраций и SLA целевые прогоны завершились 4/4, 12/12, 3/3 и 1/1. Повторный полный прогон пяти модулей 24.09.2026: 187/187, 0 ошибок, 0 пропусков, 343,567 с. BG19 на равные метки был отдельно запущен 0/2 и исключён из регистрации из-за подтверждённого дефекта продукта по указанию пользователя; его исход не скрыт. Зависший запуск с неверным форматом фильтра tests был остановлен без отчёта и не засчитан.
- [x] Разобрать чувствительность критичных утверждений к ошибке границы, чужому ID, неверному индексу и раннему хэшу. Фактические мутационные эксперименты требуют отдельного разрешённого scope и инструментария.

Разбор силы утверждений: замена строгой границы удаления на включающую обнаруживается парой записей Порог-1/Порог; подмена ID позиции чужой операции обнаруживается проверкой сохранённой метки 777000; смещение индекса транспортного ответа после пропущенного дубля обнаруживается разными статусами трёх исходных ключей; ранняя запись хэша после отрицательного ответа обнаруживается отсутствием хэша и успешной повторной отправкой (две фактические попытки). Для BG19 равные метки воспроизводящий тест существует, но отключён из-за подтверждённой ошибки продукта. Это логический разбор, мутационный запуск не выполнялся.
- [x] Устранить повторную проверку одного контракта и дублирование подготовки внутри нового набора, сохранив проверки разных опасных исходов.

Ревью 187 запусков и регистраций выше порога: девять строк срока очистки различают уникальные направление/статус, восемь вариантов разбиения пакета различают пустой, полный пакет, хвост и лимит потоков. Отправочные сценарии отдельно проверяют общий отказ, частичный отказ, исключение, ошибку сохранения и успешный дубль; объединение скрыло бы разные опасные исходы. Повторная подготовка уже вынесена в локальные помощники раздела 3. Семь запусков сверх ориентира 180 оставлены по этим различиям.

Исторические наблюдения v1 относятся к рискам следующих контрактов: R01 → BG15; R02 → BG12; R03 → BG08; R04 → BG18; R05 → BG21; R06 → BG09; R07 → BG10; R08 и R10 → BG22; R09 → BG19. Проверять эти риски на текущей ревизии в рамках соответствующих контрактов. Пометка «исправлено» из v1 не заменяет актуальную проверку. Подтверждённый дефект получает отдельный воспроизводящий тест без размножения по всем операциям. Перенос старых тестов и отдельный отчёт об отличиях не требуются.

## 8. Порядок внедрения и приёмка

Решение пользователя от 24.09.2026: пункт выполнен, когда все требуемые тесты добавлены и проверены, а ошибки тестов исправлены. Подтверждённые ошибки продукта фиксируются; блокирующий тест пропускается с сохранением воспроизводящего кода. Замеры покрытия пользователь выполняет самостоятельно; их отсутствие не препятствует закрытию пункта.

- [x] Этап 1: проверка актуальных точек входа и доступных фикстур; реализация проверок позиции/пакетов/отправки и стабильности раздела операции, калибровка бюджета по новому набору.

Этап 1 выполнен: владелец/точки входа и фикстуры сверены в разделах 3 и 6; BG03–BG05, BG09–BG11, BG23–BG24 реализованы и входят в прогон 187/187. Бюджет, длительность и объём подготовки зафиксированы.
- [x] Этап 2: получение, дедупликация, очистка и сохранение — остальные P0; реальные проверки ИБ.

Этап 2 выполнен: BG06–BG08, BG12–BG14, BG17–BG19 и BG22 проверяют получение, дедупликацию, очистку, позиции и сохранённое состояние ИБ. Для BG19 равные метки оформлены отдельным подтверждённым дефектом продукта с закомментированной регистрацией по решению пользователя; остальные зарегистрированные проверки прошли 187/187.
- [x] Этап 3: P1 и стендовые контракты; устранение дублирующей подготовки и повторных проверок.

Этап 3 выполнен: BG01–BG02, BG15–BG16, BG20–BG21, BG24 и BG-S01–BG-S04 закрыты целевыми и полным прогоном; реальные ФЗ, таймаут и подменённые транспортные границы проверены. Ревью подготовки и повторов записано в разделах 3 и 7.
- [x] Для текущих изменений запустить затронутые тесты и полный основной/стендовый набор. При будущем слиянии или выпуске повторить соответствующие прогоны.

Проверено по текущему состоянию: после изменений запускались целевые выборки 4/4, 12/12, 3/3 и 1/1, затем полный набор пяти модулей 187/187, включая реальные ФЗ и пятиминутный таймаут. Слияние и выпуск в этой задаче не выполнялись.
- [x] При недоступном обязательном уровне писать «проверка не выполнена». Падения тестов разбирать; подтверждённую ошибку продукта фиксировать с воспроизводящим тестом и пропускать согласно решению пользователя.

Все применимые уровни текущей согласованной модели выполнены. BG19 отдельно запущен 0/2, причина локализована в продуктовой границе равных меток; регистрация закомментирована по указанию пользователя, тело сохранено для повтора после исправления. Внешняя Kafka исключена пользователем из модели BG-S03; фактическая доставка брокеру не заявляется.
- [x] Для приёмки все требуемые тесты добавлены, проверены и ошибки тестов устранены. Подтверждённые ошибки продукта перечислены как исключения с результатом воспроизводящего запуска.

Критерий пользователя выполнен для текущей ревизии: зарегистрированный набор 187/187, диагностика пяти модулей 0 замечаний. Ошибки тестового кода SLA устранены. Единственное согласованное исключение — BG19 равных меток с воспроизводящим результатом 0/2 и сохранённым отключённым тестом. Замер покрытия выполняет пользователь отдельно и приёмку не задерживает.
- [x] Для BG01–BG24 и BG-S01–BG-S04 заполнены тест, вход, ожидаемое состояние, уровень, результат и ревизия. Все P0 закрыты содержательными утверждениями и обязательным уровнем проверки.

### Трассировка контрактов текущей ревизии

Ревизия для каждой строки: adapter/adapter dc3c6740ddf429782ec608653aa8b24c9d6567fb; adapter/base af62a80f6be3abd949c20268734c8fcb3a99dbc5; tests/unit/unit 928e48eaeff46ef6059592f3bc0a7e47e9c6a785 + текущее рабочее дерево. Результат «187/187» означает прохождение соответствующего зарегистрированного теста в полном прогоне пяти модулей 24.09.2026.

| Контракт | Тест | Различающий вход | Ожидаемое состояние | Уровень | Результат |
|---|---|---|---|---|---|
| BG01 | ЗапуститьРегламентноеЗадание_УчитываетОпции; ЗапуститьФЗ_ПерезапускаетПослеПрерывания | Опции и блокировка включены/выключены, активное ФЗ | Нет лишнего запуска; разрешённый запуск и повтор сохраняют работу | BSL, ФЗ | 187/187 |
| BG02 | СписокСлужебныхОпераций_УчитываетНезависимыеФлаги | Четыре одиночных флага и совместное включение | Точный состав операций без лишних | BSL | 187/187 |
| BG03 | ОбновитьУказательНачалаВыборки_УчитываетПрефикс | Два пакета с четырьмя состояниями завершения | Позиция проходит только завершённый префикс; чужая позиция неизменна | ИБ | 187/187 |
| BG04 | ОбновитьУказательНачалаВыборки_ИсчезнувшийПотокУдерживаетПозицию; ОбновитьУказательНачалаВыборки_РавныеМеткиНеСкрываютНезавершенныйПакет | Исчезнувший поток, равные метки и перезапуск | Остаток остаётся назначаемым, позиция не теряет его | ИБ, ФЗ | 187/187 |
| BG05 | РаспределитьВыборкуПоПотокам_СохраняетКлючи | Пусто, полный лимит, хвост, разные числа потоков | Каждый исходный ключ встречается ровно один раз | BSL, ИБ | 187/187 |
| BG06 | ВыборкаДесериализацииВПотоке_УчитываетКлючДатуИПоток; ВыборкаИсходящих_ОтбираетДопустимыеЗаписи | Чужой владелец, дата, UUID, статус, занятость | Выбраны только допустимые ключи в нужном порядке | ИБ | 187/187 |
| BG07 | Сериализация_СохраняетСогласованныйИсход | Успех, отказ, исключение, нет тела/правила/метода | Статус, тело, журнал, счётчик и назначение согласованы | ИБ | 187/187 |
| BG08 | Десериализация_ДваСообщенияНеСмешиваютСвойства; СериализацияКД_ПрерываетРегистрПриОшибке | Два разных сообщения и ошибка регистра | Данные не смешаны, ошибка остановила обработку | ИБ, обработчик | 187/187 |
| BG09 | Отправка_СопоставляетОшибкуПослеПропуска | Пропуск, успех и ошибка в одном пакете | Статусы относятся к исходным ключам | ИБ, мок Kafka | 187/187 |
| BG10 | Отправка_РазрешаетПовторПослеОшибки; Отправка_ЧастичнаяОшибкаНеПодтверждаетХэш | Общий/частичный отказ и успешный повтор | Хэш после отказа отсутствует, повтор доступен | ИБ, мок Kafka | 187/187 |
| BG11 | Отправка_ОшибкаСохраненияНеПодтверждаетЗавершение | Успех транспорта и отказ записи | Очередь и хэш не подтверждены ложно | ИБ, мок Kafka | 187/187 |
| BG12 | ДобавитьВходящееСообщениеВОчередь_СохраняетПолезнуюНагрузку; ДобавитьВходящееСообщениеВОчередь_РазличаетПустойИОтсутствующийКлюч | Двоичное тело, headers, пустой/отсутствующий key | Запись ИБ и признак загрузки сохраняют идентичность | ИБ | 187/187 |
| BG13 | ОбработчикЗагрузкиПакетСообщений_ОшибкаЗаписиНеСохраняетПакет | Чтение с автофиксацией, отказ записи | В ИБ нет строки, Загружено = Ложь, ошибка видна | ИБ, мок Kafka | 187/187 |
| BG14 | ОбработатьВходящееСообщение_ХэшПоявляетсяТолькоПослеУспеха | Первая обработка успешна или ошибочна | После успеха дубль подавлен, после ошибки допускается | ИБ | 187/187 |
| BG15 | РаспределитьКонсьюмеровПоПотокам_УчитываетНагрузку; ОбновитьРаспределениеКонсьюмеров_СохраняетРабочееСостояние | Нагрузка до/на пороге, повторная инициализация | Распределение и рабочее состояние сохранены | BSL, ИБ | 187/187 |
| BG16 | ЦиклДесериализации_УчитываетБлокировкуИПоступление; ЦиклДесериализации_ПустойПотокЗавершаетсяПоТаймауту | Блокировка до/между порциями, новое сообщение, пустой поток | Правильные статусы; пустой ФЗ завершён штатно | Реальное ФЗ | 187/187 |
| BG17 | ВыборкаВозвратаВОчередь_ПроверяетГраницуИПопытки; ОбработатьВыборкуДублей_ОтменяетТолькоСтаруюРегистрацию | Возраст, лимит попыток, новый дубль | Изменены только допустимые записи | ИБ | 187/187 |
| BG18 | ВыборкаУстаревшихСообщений_ПрименяетСрокКСвоемуСтатусу; УдалитьЗаписиРС_УдаляетПолныйПакетИХвост | Сроки -2/-1/0, до/на пороге, полный пакет и хвост | Удалены только устаревшие записи своего статуса/направления | ИБ | 187/187 |
| BG19 | Логирование_ОшибкаУдерживаетПозицию; Логирование_НеПерескакиваетРавныеМетки | HTTP 500/исключение; равные метки на границе порции | При отказе позиция удержана; хвост равных меток должен остаться | ИБ, мок HTTP | Отказ 4/4; равные метки 0/2, дефект продукта, регистрация отключена |
| BG20 | Логирование_СохраняетСобытие; ЛогированиеHTTP_ПередаетСобытияИОбрабатываетОтказ | Входящее/исходящее событие, POST и gzip | Событие соответствует эталону, отказ HTTP распознан | ИБ, мок HTTP | 187/187 |
| BG21 | КонтрольSLA_УчитываетВсеПоказатели; КонтрольSLA_ФормируетСообщениеПоПоказателям | Четыре показателя, разные диапазоны, повтор | Правильные счётчики, единицы времени и сообщение | ИБ | 187/187 |
| BG22 | НоваяОчередь_ОчищаетСтрокиИОтбор; ХэшСуммаТела_СоответствуетЭталону | Два последовательных вызова, пять эталонов SHA256 | Нет старых строк/отбора/метки; хэш совпадает | BSL, ИБ | 187/187 |
| BG23 | ПакетKafka_СохраняетАдресациюИТело; ПакетKafka_ОтклоняетОтсутствующийТопик | Пустой/заданный key, нет topic | На границе точные topic/key/partition/тело/headers; без topic ошибка | ИБ, мок Kafka | 187/187 |
| BG24 | ПолучитьРазделОперации_НеЗависитОтПорядка; ПолучитьРазделОперации_РазличаетСостав | Перестановка и замена настройки | Раздел стабилен при перестановке, различен при замене | BSL | 187/187 |
| BG-S01 | ОбновитьУказательНачалаВыборки_РеальныеПотокиЗавершаютсяВОбратномПорядке | Два ФЗ завершены в обратном порядке | Межсеансовая позиция проходит завершённый префикс | Реальные ФЗ | 187/187 |
| BG-S02 | ЗапуститьФЗ_ПерезапускаетПослеПрерывания | Отмена ФЗ и повторный запуск | Остаток сохранён, лишнего задания нет | Реальные ФЗ | 187/187 |
| BG-S03 | ПакетKafka_СохраняетАдресациюИТело; ОбработчикЗагрузкиПакетСообщений_ОшибкаЗаписиНеСохраняетПакет | Отправка и отказ записи после чтения | Ответ и аргументы мока верны; нет ложной строки ИБ | ИБ, мок Kafka | 187/187; брокер исключён |
| BG-S04 | ЛогированиеHTTP_ПередаетСобытияИОбрабатываетОтказ; ОповещениеTelegram_ИзолируетHTTPСоединение | HTTP 200/500, Telegram | POST/gzip/события и обработка отказа соответствуют эталону | Мок HTTP | 187/187; сеть исключена |

- [x] В полном наборе тестами проверены оба исхода критичных решений либо для подтверждённой ошибки продукта оформлено исключение. Замер строк/ветвей выполняет пользователь отдельно и его отсутствие не задерживает закрытие.

Карта исходов раздела 1 сопоставлена с таблицей трассировки: префикс/остаток, успех/отказ отправки, до/на границе удаления, успешная/ошибочная дедупликация и продвижение/удержание позиции имеют содержательные утверждения в наборе 187/187. Исключение BG19 с равными метками документировано отдельно. Инструментальное покрытие пользователь измерит самостоятельно; отсутствие замера не влияет на закрытие.
- [x] Известные дефекты и ограничения текущей ревизии перечислены отдельно; подтверждённая ошибка продукта допускает закрытие пункта по решению пользователя.

### Известные дефекты и ограничения текущей ревизии

- BG19: продукт теряет хвост логов с равной миллисекундной меткой через границу порции. Воспроизводящий тест для двух направлений дал 0/2; регистрация закомментирована по указанию пользователя, тело сохранено до исправления продукта.
- BG13/BG-S03: чтение Kafka выполняется с автофиксацией. При отказе записи в ИБ фактический offset брокера может продвинуться; тест подтверждает ошибку, отсутствие записи и Загружено = Ложь на подменённой границе. Фактическая доставка и offset брокера не заявляются.
- BG-S04: HTTP и Telegram проверены на подменённой сессии. Реальный сетевой приём и независимая распаковка gzip не входят в согласованную модель.
- Стендовый пустой поток ожидает штатный таймаут около 300 секунд; это основная стоимость полного прогона.

- [x] Для нового набора зафиксированы число запусков, примеры генерации, время подготовки/исполнения и наиболее дорогие фикстуры. Подготовка остаётся компактной, повторные проверки одного контракта устранены. Сравнение показателей со старым набором не требуется.

Метрики раздела 3: 81 процедура, 144 строки параметров, 187 запусков, 0 генеративных примеров, 343,567 с повторного полного прогона. Наибольшая стоимость — реальный пятиминутный таймаут, а не объём записей; проверенные фикстуры ограничены несколькими собственными строками. Различия сверх ориентира 180 и отсутствие дублей разобраны в разделе 7.
- [x] Текущие изменения тестов проверены по паттернам YAxUnit, диагностике `unit-edt` до/после и целевым/полному запускам. Для асинхронного запуска получен конечный результат того же задания; неожиданные 0 тестов считаются ошибкой.

Сверены паттерны registration-and-parameters, data-isolation и test-module: fluent-регистрация ЮТТесты, экспортные серверные тесты и транзакция по подходящей области. Диагностика пяти изменённых модулей после правок: 0 замечаний. Финальное асинхронное задание 717f639f-3ae1-4979-bffe-23d3686893a8 завершилось 187/187; нулевых выборок нет. Для следующих изменений этот же порядок остаётся правилом.

Нормативная основа проектирования: корпус `v8std`, паттерны YAxUnit `authoring-baseline`, `test-module`, `naming`, `registration-and-parameters`, `assertions`, `test-data`, `data-isolation`, `mocking`, `dependencies`, `lifecycle-and-contexts`. При реализации дополнительно выбрать требования к реально изменяемым конструкциям BSL. Изменения тестов выполнены только в unit через unit-edt; инструментальное покрытие этим планом не подтверждается и остаётся за пользователем. Соседние репозитории не изменялись.
